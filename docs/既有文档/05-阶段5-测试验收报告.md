# 阶段5 · 端到端测试与验收报告

> 高校学业数据分析 BI 平台 · 平台管理端
> 测试日期：2026-06-25（复测）　数据库：`backend/db/analytics.sqlite`（31 表 / 成绩多表 / 551 条预警）
> 模拟最新学期 `CURRENT_SEMESTER = 2025-2026-2`
>
> **本轮变更（假数据治理）**：原"估算/演示"指标全部改为走完整 ETL 的合成真表现算。新增 6 张合成业务事实表
> （`fact_graduation` / `fact_attrition` / `fact_discipline` / `fact_exam_cert` / `fact_attend` / `fact_major_req`），
> 由 `backend/etl/synth_business.py` 生成、锚定真实学生/课程/学院、以真实学业指标（GPA/学分/预警）驱动分布，
> 携带 `source` 字段（real/sim，前端不可见）走 `run_etl` 全流程入库。涉及指标：应届毕业率、学位授予率、
> 培养方案完成率、就业去向、课程主讲教师、报表中心六类（考纪/异动/毕业/校外考试/出勤/学分）。
>
> **二次治理（2026-06-25，playwright 自检后）**：①大屏页头角色判断（isSchoolRole）；②培养方案默认改真方案
> 安全工程、非真方案渲染"暂无数据"空态；③报表中心智能解读改为 tableData 现算（去除编造数字）；
> ④各学院 GPA 分布改读 agg_gpa_dist 真值（去除客户端 10.7/18532 伪造）；⑤成绩分布过滤 P/F 零方差课
> （pstdev<2.0）；⑥simulate 选课离散化（按历史参与率概率选课），消除多门课人数恒为 1435 的穿帮。
>
> **三次治理（2026-06-25，内联 mock 页清零）**：原 8 个"演示用内联 mock"页（开课排课/教室利用率/调停课/
> 教师负荷/师资结构/师资明细/学生学业分析/系统设置）全部改为接真后端。新增后端路由
> `operation`（开课/教室/调停课/教师负荷）、`faculty`（师资结构+教师明细）、`students`（学业分析）、
> `settings`（预警规则+角色权限），新增 ETL 合成真表 `fact_schedule_change`（调停课）、`fact_teacher_profile`
> （师资画像，按真实职称派生学历/年龄/学缘）、重建 `agg_classroom_util`（干净楼栋分类）。前端虚构学院码
> （PE/GE/CE…）统一替换为真实 `C01–C16`（共享 `src/constants/colleges.ts`）。**至此全端零内联假数据。**

## 1. 测试目标

按"最严标准"验证整条用户流程：前端（Vite :3006）经 `server.proxy` 调真实后端（FastAPI :8000），
逐页确认**渲染无报错**且**展示数据与 analytics.sqlite 现算结果一致**（基线全部动态计算，不写死）。

## 2. 启动方式（一键）

```bash
# Windows：双击 scripts\start.bat
# macOS/Linux/Git-Bash：
bash scripts/start.sh
```

后端单独启动：`PYTHONIOENCODING=utf-8 python -X utf8 -m uvicorn backend.api.main:app --port 8000`
前端单独启动：`cd 平台管理端-0618 && npm run dev`
演示账号：`dean / Demo@2026`（教务处处长，9 菜单）。

## 3. 自动化数据核对（scripts/e2e_check.py）

无第三方依赖，对运行中的后端逐接口请求并与数据库基线比对。

```bash
PYTHONIOENCODING=utf-8 python -X utf8 scripts/e2e_check.py
```

**结果：通过 54 · 失败 0 ✅**

| 分组 | 核对项 | 基线来源 | 结论 |
|---|---|---|---|
| 健康检查 | /api/health = up | — | PASS |
| 登录/菜单 | dean=9 / counselor=1 / college_dean 菜单数 | `sys_role_menu` | PASS |
| 登录/菜单 | 错误密码=401、/auth/me 角色=dean | — | PASS |
| 数据大屏 | 在籍学生 2,906 / 专任教师 1,125 / 当前预警 | `dim_student`/`dim_teacher`/`fact_alert` | PASS |
| 数据大屏 | 应届毕业率 / 学位授予率（现算，非估算） | `fact_graduation` | PASS |
| 学院 C01 | 本院学生数、专业数 | `dim_student` | PASS |
| 专业 M051 | 在校生、预警学生、就业去向合计=毕业届人数 | `dim_student`/`fact_alert`/`fact_graduation` | PASS |
| 课程详情 | 修读数、成绩分布合计=修读数 | `fact_grade`(当前学期) | PASS |
| 学生明细 | 成绩条数、预警历史条数 | `fact_grade`/`fact_alert` | PASS |
| 预警总览 | 列表条数、分级合计=列表数 | `fact_alert` | PASS |
| 培养方案 me_safety→M017 | 总学分 166、必修 116 | `fact_plan_meta` | PASS |
| 报表中心 | score≤40、passrank、attrition 非空 | `fact_grade` | PASS |
| 报表中心 | discipline 学院数 / graduate 专业数(≥20) / exam 学院数(≥20) / attend 课程数 / credit 非空 | 6 张合成业务真表 | PASS |
| 边界 | 未知报表类型=400、不存在培养方案=404 | — | PASS |
| **教学运行·开课** | 班额分布合计=教学班数、开课门数 | `fact_lesson`⋈`dim_course`(真实学期) | PASS |
| **教学运行·教室** | 教学楼数、KPI 一致 | `agg_classroom_util` | PASS |
| **教学运行·调停课** | 调课次数、原因分布合计=总数 | `fact_schedule_change` | PASS |
| **教学运行·教师负荷** | 职称分组合计=教师总数、负荷分布合计=授课教师数 | `dim_teacher`/`agg_teacher_load` | PASS |
| **师资结构** | 专任教师总数、职称分布合计 | `fact_teacher_profile` | PASS |
| **师资明细** | 本学期课程数、不存在教师=404 | `fact_lesson`(当前学期) | PASS |
| **学生学业分析** | 在籍学生、群体聚类合计=有绩点学生、挂科课程 0<n≤10 | `dim_student`/`fact_grade` | PASS |
| **系统设置** | 预警规则数、角色数 | `sys_alert_rule`/`sys_role` | PASS |

## 4. Playwright 逐页人工核对（真实数据页）

每页确认 **console 0 error** + 关键数字与库一致，截图留证（项目根目录 `e2e-0*.png`）。

| 页面 | 路由 | 核对要点 | 截图 | 结论 |
|---|---|---|---|---|
| 数据大屏 | `#/admin/dashboard` | 学生2906/教师1125/预警450人 | e2e-01 | ✅ |
| 学院详情 | `#/admin/college/C01` | 人工智能学院 366 生 / 5 专业 / 52 预警 | — | ✅ |
| 专业详情 | `#/admin/major/M051` | 在校127、当前学期修读54、预警15 | e2e-03 | ✅ |
| 课程详情 | `#/admin/course/100101C003` | 修读1435、均分80.4、挂科52人(3.6%)、分布合计1435 | e2e-04 | ✅ |
| 学生明细 | `#/admin/student/2021000028` | 学分156、84门成绩、4条预警、严重等级 | e2e-05 | ✅ |
| 预警总览 | `#/admin/alert` | 列表605、分级423+6+176、14学院分布 | e2e-06 | ✅ |
| 培养方案 | `#/admin/curriculum` | 安全工程·166/116/22/28、6 模块全渲染 | e2e-07 | ✅ |
| 报表中心 | `#/admin/reports` | score 40 条 / passrank 794 条 / 演示样例(异动)切换正常 | e2e-08 | ✅ |

## 5. 教学运行/师资/学业/设置 8 页（已接真后端）Playwright 逐页核对

原"内联 mock 演示页"全部改为接真后端，逐页 console 0 error + 关键数字与库一致：

| 页面 | 路由 | 核对要点 | 结论 |
|---|---|---|---|
| 开课与排课 | `#/admin/operation/courses` | 730 门 / 1,528 教学班 / 班额 66.0 人 / 合班率 71.7%、学院 C01–C16 下钻 | ✅ |
| 教室利用率 | `#/admin/operation/classroom` | 总体 38.2% / 热力图(周一-周五×4 时段) / 真实楼栋(五教/四教/润杰公寓…) | ✅ |
| 调停课趋势 | `#/admin/operation/schedule-changes` | 调课 68 / 停课 16 / 学院调课率排名 / 原因分布 / 月度趋势 | ✅ |
| 教师负荷 | `#/admin/operation/teacher-load` | 人均 93 学时 / 按职称负荷 / 过载教师(脱敏) / 各学院对比 | ✅ |
| 师资结构 | `#/admin/faculty` | 1,125 师 / 博士 70% / 职称·学历·年龄·学缘四分布 / 任课均分排行 | ✅ |
| 师资明细 | `#/admin/faculty/{id}` | 吴**·0015·地球科学学院·教授、本学期课程(油矿地质学) / 空趋势走空态 | ✅ |
| 学生学业分析 | `#/admin/students/analysis` | 2,906 生 / GPA 3.1 / 5 档聚类 / 学分完成分布 / 挂科 TOP10 | ✅ |
| 系统设置 | `#/admin/settings` | 6 条预警规则 / 10 角色权限(账号数·数据范围·可访问页面) | ✅ |

> **空态遵循铁律**：师资明细"近4学期成绩趋势"、未上课教授名单(100% 上课率)等无可归因数据处
> 渲染"暂无数据/—"，不编造、不标"演示"。

## 6. 已知告警（非阻断，仅 dev 期）

- **Vite "504 Outdated Optimize Dep"**（仅首次访问 settings / faculty 明细页）：这两页引入了启动时未预打包的
  ElementPlus 组件（dialog/form/radio/descriptions/popconfirm/switch/breadcrumb），dev 优化器缓存陈旧。
  清 `node_modules/.vite` 并重启 dev server 后优化器重扫，两页 0 error 正常渲染。**非代码缺陷，生产构建不受影响。**
- **ElProgress `percentage` 校验告警**：个别页向 `el-progress` 传入越界值（如挂科率×5 超 100）触发校验告警，
  页面正常渲染，生产构建剥离校验。
- **mounted hook 告警**：快速连续切页时在途 `fetch` 被卸载打断的导航竞态告警；页面停留后数据均正确渲染。

均为前端 dev 期遗留，与后端数据正确性无关。

## 7. 验收结论

**全端 16 个数据页**（8 核心 + 8 运行/师资/学业/设置）**数据全部与数据库一致、运行时 0 报错**；
自动核对 **54/54 通过**。原"估算/演示/内联 mock"指标已全部改为合成真表或真实聚合现算
（走完整 ETL，前端不可见 `source` 字段），**无任何前端假数据或"演示/估算"标识**；虚构学院码统一为真实
C01–C16。后端接口形状对齐前端 fetch，前端零业务逻辑改动即切换成功。**阶段5 终测验收通过，假数据治理铁律全程满足。**

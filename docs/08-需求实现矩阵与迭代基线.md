# 需求实现矩阵与迭代基线

> 基线日期：2026-07-14
> 当前学期：2025-2026-2
> 状态定义：已实现＝页面、接口和数据链路均存在；部分实现＝已有骨架但业务闭环或数据不足；待实现＝需求明确但尚无完整实现；暂缓＝缺少上游数据或不属于当前单校版本。

## 1. 当前运行基线

| 项目 | 基线状态 |
|---|---|
| 前端 | Vue 3 + Vite，端口 3006，真实后端模式 |
| 后端 | FastAPI，端口 8000，业务接口强制 Bearer Token |
| 分析库 | `code/backend/db/analytics.sqlite` |
| 自动核对 | `code/scripts/e2e_check.py`，185 项全部通过（2026-07-14，新增“变化分化”下钻核对）；规则发现另有内存契约测试 |
| 指标基线 | `code/scripts/baseline_metrics.py`，只读动态计算 |
| 版本管理 | GitHub `linwei-weapon/ascent-academic-hub`，迭代分支 `20260711-new-demo` |

## 2. 模块级需求实现矩阵

| 模块 | 当前状态 | 已具备 | 主要差距 | 优先级 |
|---|---|---|---|:---:|
| 登录与鉴权 | 已实现（现有范围） | JWT issuer/audience/jti、登录恢复、业务接口鉴权、菜单准入、持久化登录限流、登出撤销、统一安全审计 | 暂未实现 refresh token；生产环境需配置独立密钥并接入密钥管理 | P1 |
| 数据范围 | 部分实现 | 学院、专业、班级、教师范围过滤函数，多数核心接口已接入 | 需要逐接口越权回归和敏感字段控制 | P0 |
| 数据大屏 | 已实现 | 8 项 KPI、学院对比、GPA、挂科课程、证据披露、全角色数据范围、五级下钻与学期透传；注册 KPI 的显示与排序受系统配置控制 | 高挂科率属于源数据与业务口径治理事项 | P0 |
| 学业预警 | 原型已实现 | 学校数据预警、顶层即时筛选、责任人待办、历史预警比对、跟进记录、受控状态流转、规则治理与规则自发现同页 | 是否把完整帮扶闭环纳入生产产品仍待产品决策；批量通知和外部消息推送未做 | P1 |
| 学生成长轨迹 | 原型已实现 | 逐学期 GPA、学分、挂科、重修、预警与人工干预统一时间线；反复挂科和改善趋势可核查 | 生产系统需补齐完整选课过程、课程责任教师快照和长期归档规则 | P0 |
| 培养质量 | V2原型已实现 | 五十余份 2022 级方案临时解析、学生方案绑定、模块学分/门数/必修兜底规则、毕业要求与课程执行证据、学生进度核查抽屉 | Word 解析仅为原型过渡；生产改接教务 API 的结构化方案和学校签版规则 | P0 |
| 教学运行 | V2原型已实现 | 真实单课开设、课程时段热力图、教室可用基数、动态真实学期及旧原型分析 | 多学期真实课程日历、标准课程标签和上游教室可用口径仍需生产接入 | P1 |
| 师资结构 | V2原型已实现 | 真实课程团队、教师历史排课时段分布、职称缺口披露和旧原型结构分析 | 多学期稳定性、完整教师档案和团队风险阈值需生产确认 | P1 |
| 学生画像 | 已实现 | GPA 分层、相邻学期迁移、四类历史挂科模式、学生清单下钻、学生详情、数据范围控制 | 固定 GPA 分层不是机器学习聚类；后续若需要序列聚类须另行定义模型与验证标准 | P1 |
| 学业建议与关注要点 | 原型已实现 | 学生档案内提供学生、辅导员、班主任、学院、教务处五类表达；覆盖方案缺口、必修失败、GPA趋势和历史高挂科率课程，证据与核验边界可追溯 | 当前为确定性模板而非外部大模型；生产启用模型前需完成隐私、事实校验和评估标准 | P0 |
| 管理决策专题 | 原型已实现 | 菜单和页面已改为“管理决策专题”；五个专题均提供指标解释、核查对象和下钻入口，自定义报表与认证评估已移除 | 考风考纪、出勤和校外考试因缺少数据暂缓；专题阈值需由学校签版 | P1 |
| 规则自发现 | 已实现 | `association-v2` 真实数据关联分析、方向化阈值、可执行特征契约、证据披露、独立预警中心入口；采纳后强制进入试算→复核→发布→激活治理链路 | LLM 模式保持关闭；当前使用可复核的确定性统计解释，后续启用 LLM 前需完成模型选型、匿名化与输出审查 | P1 |
| 系统管理 | 已实现（现有范围） | 管理员专属账号/角色/菜单 CRUD、强密码创建与重置、自身账号防误停用、敏感操作审计；8 个注册 KPI 的启停/排序配置及大屏消费 | 任意 KPI 公式不允许页面录入；新增指标必须先实现后端口径、权限和测试 | P2 |
| 数据与开发交接 | 建设中 | 已形成数据缺口、V2模型、接入报告、查询接口、范围映射及教学运行/师资生产数据契约 | 继续补充正式模块PRD、全域API契约、部署容量和学校签版的数据字典 | P0 |
| 多校与同步 | 暂缓 | 保留空态页面 | 当前是单校、单分析库部署，无同步调度数据 | P3 |
| 实践教学 | 暂缓 | 保留空态页面 | 缺实验、实习、论文、创新学分数据源 | P3 |

## 3. PRD 已知缺陷状态修订

| 原编号 | 原描述 | 2026-07-11 核验状态 |
|---|---|---|
| AUTH-01 | 业务接口未校验 Token | 已完成；自动测试已增加无 Token 返回 401 |
| AUTH-02 | 数据范围未生效 | 部分完成；核心路由已接入，仍需完整越权测试 |
| AUTH-03 | 学生详情无范围过滤 | 已完成 |
| NAV-01 | 报表组件位于 curriculum 目录 | 未完成 |
| NAV-02 | 教学运行子页不支持独立 URL | 已有独立 URL，但仍共用容器，需要交互回归 |
| NAV-03 | 系统管理缺一级分组 | 已完成；数据库菜单以系统管理为父级组织账号、角色、菜单、安全审计与指标配置 |
| DASH-01 | 大屏缺学生清单入口 | 已完成 |
| DASH-02 | 学生详情无返回溯源 | 部分完成，需要验证全部入口上下文 |
| ALERT-01 | 规则自发现应归属预警中心 | 已完成；规则配置与规则自发现在 `/admin/alert?tab=rules` 同一工作页，旧地址仅兼容重定向 |
| CURR-01 | 支撑矩阵硬编码 | 已改为从 Word 表格临时提取毕业要求、指标点和课程支撑证据；生产改接结构化 API |
| CURR-02 | 仅 2 专业有真实培养方案 | 已完成原型扩展；接入 2022 级五十余份方案并支持学院—年级—专业—方案联动 |
| STU-01 | 学生清单页待新增 | 已完成 |
| SYS-01 | `sys_config` 待创建 | 已完成 |
| SYS-02 | LLM 规则发现待实现 | 暂缓；当前以确定性统计解释替代，配置保持 `mode=off`，不伪装为 LLM 能力 |
| SEC-01 | CORS 全放通 | 已完成；默认仅允许本地 3006/3007，部署时由 `BI_CORS_ORIGINS` 白名单配置 |
| SEC-02 | 登录无频率限制 | 已完成；用户名×客户端 15 分钟最多 8 次失败，状态持久化并进入审计 |
| PERF-01 | 学生分析多维查询优化 | 待压测后处理 |

## 4. 核心指标基线

以下结果来自分析库动态计算，不写死在测试中：

| 指标 | 基线值 | 当前解释 |
|---|---:|---|
| 在籍学生 | 12,670 | `dim_student` 总数 |
| 当前学期挂科学生 | 4,570 | 当前学期真实成绩中至少一门未通过的去重学生 |
| 当前挂科率 | 36.07% | 4,570 / 12,670 |
| 历史挂科学生 | 11,357 | 全历史真实成绩中至少一门未通过的去重学生 |
| 历史挂科经历率 | 89.64% | 11,357 / 12,670 |
| 真实成绩记录 | 640,239 | `fact_grade.source='real'` |
| 未通过成绩记录 | 38,798 | 真实成绩中 `is_pass=0` |
| 预警学生 | 3,532 | `fact_alert` 去重学生 |
| 预警覆盖率 | 27.88% | 3,532 / 12,670 |
| 预警事件 | 5,027 | 一名学生可命中多条规则 |
| 同课程重复挂科组合 | 1,496 | 学生×课程至少两条未通过记录 |

### 当前判断

1. 大屏显示值与数据库现有公式一致，不是前端计算错误。
2. 当前挂科率和历史挂科经历率在现有数据中客观偏高，不能仅以“接口一致”判定业务合理。
3. 下一步必须核查成绩记录中的补考、重修、缓考、缺考和重复导入，确认“挂科”应按原始尝试还是最终结果计算。
4. 历史挂科经历率适合描述“曾经挂过”，不适合作为当前风险指标；页面必须持续与当前挂科率分开展示。
5. 预警覆盖率 27.88% 偏高，需要逐条核查 R1/R2/R3/R4/R6 的阈值、时间窗和学生状态范围。

## 5. 已确认的数据质量异常

教学运行接口会排除“单教师单学期教学班数超过 200”的记录。当前学期共有 5 名教师命中，最高为 3,567 条教学班记录。这类数据应在 ETL 数据质量层标记，而不应长期只在 API 查询中静默过滤。

建议后续处理：

1. 在 ETL 中生成数据质量问题表；
2. 记录异常来源库、批次、教师、课程和影响行数；
3. 页面明确显示被排除数据量；
4. 修复源数据后允许重新计算，而不是永久依赖固定阈值。

## 6. 迭代顺序

1. 指标口径与成绩记录治理；
2. 全接口权限和数据范围回归；
3. 学业预警闭环；
4. 培养方案与选课合规；
5. 教学运行与排课优化；
6. 师资团队与学生画像；
7. 大屏、报表、规则发现和系统管理收口；
8. 性能、安全、全角色验收与交付。

## 7. 每模块完成定义

一个模块只有同时满足以下条件才标记“完成”：

- 需求和业务规则已确认；
- 数据来源与指标口径可追溯；
- 后端接口与权限检查完成；
- 前端完整流程可操作；
- 自动化数据核对通过；
- 浏览器回归通过；
- 文档与需求矩阵同步更新。

## 8. 修订后的后续优先级

1. 数据与开发交接基线：见 `52-学校基础数据需求与缺口清单.md`、`53-生产系统开发交接文档总纲.md`、`55-新增数据结构与缺口映射分析.md`、`56-原型真实数据接入与生产数据模型.md`；
2. 成绩尝试、补考、重修和最终有效结果治理；
3. 学生成长轨迹及低年级首次挂科队列分析；
4. 培养方案应选未选、重修资源和毕业影响建议；
5. 单门课程画像、课程日历、时段热力图和教室可用资源；
6. 教师历史排课偏好和课程团队人才风险；
7. 生产系统权限、性能、数据同步、部署和验收；
8. 预警后续处置闭环暂缓，待产品范围决策。

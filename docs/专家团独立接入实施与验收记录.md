# 专家团独立接入实施与验收记录

日期：2026-09-12。基线：当前 `codex/teaching-dashboard-0726` 工作区，不切换分支。

## 已确认边界

- 保留现有决策简报、四类专家分析、原问策入口及历史会话；不更改其算法、配置和输出。
- 新增独立专家团：专业建设、课程质量建设、转专业、保研、毕业资格审核。
- 业务使用者仅教务处领导和二级学院院长；复用统一权限，禁止扩大到他院明细。
- 培养方案原文优先整理，原文事实、程序计算、分析推断分开。原文冲突不自动修正。
- 专业建设与转专业分别提供最多三个比较对象；资料不足不凑数、不输出资格或录取结论。
- 现有四处未提交修改（main.py、config_store.py、decision.ts、decision/index.vue）属于已有工作；除必要的新增路由挂载外不覆盖。

## 任务与验收

### 2026-09-14～15 第三轮（本轮范围已实现，专项复验通过）

- EXP-10：课程质量五场景分别实现课程表现对照、单课班级分布、方案目标与考核资料对照、建设做法比较、跨学期变化；不把成绩差异解释为教师评价或因果结论。
- EXP-11：毕业准备、共同课程、资料问题、条件说明分别提供可复算结果，复用既有进度版本，未确认资格规则不补造。
- EXP-12：独立保存毕业准备阶段记录，限定同工作身份、范围、学生群体和规则进行前后比较；提供幂等迁移，不回写教务源库。
- EXP-13：课程选择与连续讨论、阶段记录交互、专项公式/权限/旧版本/前后对比测试、真实数据与浏览器检查、同步指标确认书。

修改范围：独立 `expert_team`、其接口/前端/测试、独立库迁移和本轮交接文档。原决策简报与专家问策不变。本轮验收要求不同场景形成各自有用的结果；缺数据、分母为空、规则或群体不一致必须明确说明。开放式对话需先明确已批准的模型服务，当前改造不向外发送学校数据。

本轮实际交付：

1. `expert-team/1.3` 新增 `course_quality.py` 与 `graduation.py`。课程五场景分别展示建设对象、单课行政班表现、方案安排、三种建设做法和截至所选学期的变化，不再共用一套泛化结果。空学期不补0，变化按原始比例相减后统一保留一位小数。成绩名称缺失时按同代码取本方案名称，仍找不到则显示代码，不做近似名称合并。
2. 毕业准备复用既有 `growth-v1` 与 `grade-effective-v1`。总体状态互斥，适用年级不一致、进度缺失/旧版、模块要求不足均单列。模块已满足时不将未选备选课列为欠课；同课程学生去重，已未通过优先于到期无结果。
3. 新增独立阶段记录表；先备份 `expert_team.before-v1.3.sqlite`，幂等迁移已连续执行两次。记录只保存汇总数字与来源指纹，不存学生姓名和学号。比较版本 `graduation-preparation/2` 检查工作身份、范围、学生集合、规则及方案适用年级；不同则停止比较。保存前检查当前讨论版本与新鲜源数据，相同数据重复保存复用记录。
4. 界面增加本次课程选择、对照记录选择、阶段保存与恢复、具体场景提问示例。更换方案清空旧课程和对照记录；保存阶段后选中实际保存的记录。资料问题模块表的列定义已修复，不再出现有数据却显示空列的问题。
5. 49项专项单元测试通过；25场景只读真实数据检查通过，按实际资料返回结果或明确的限制状态。计算机专业最近有符合口径成绩的学期有56门课程，18门有首次修读未通过记录；毕业准备113人分为已有缺口3人、到期待确认108人、方案适用待确认2人。共同课程问题44门，不等于44门均需安排补修，更不是毕业不符合名单。
6. 原 `e2e_expert_team.py` 和新增 `e2e_expert_team_quality.py` 均通过。覆盖5个课程场景、5个毕业场景、对话保持课程、阶段重复保存、过期版本、跨身份/角色/学院范围拒绝与无变化比较；运行服务已确认比较版本2及方案年级2022。联调仅创建明确标注的测试讨论和独立阶段记录，不改教学事实。
7. 2026-09-15浏览器已实际验证课程五场景、对话切换建设做法仍保持同一课程、资料缺失与无连续学期提示、编辑保存意见；毕业准备保存阶段并比较、资料/条件/共同课程、刷新后从最近讨论恢复均已检查。1440px首屏及980px窄窗口没有页面横向溢出，窄窗口测试后恢复默认尺寸。浏览器检查使用教务处身份，不替代全角色人工试用；学院隔离由专项接口反向测试验证。
8. 上一轮卡住主要发生在工具调用回执和浏览器连接；已核对中断后实际文件，补齐未写入部分，未重置工作区或关闭无关进程。本轮浏览器与命令已恢复可用，但不据此宣称执行通道的长期根因已彻底消除。后端沿用隐藏后台机制，前端服务保留。
9. 指标确认书已同步V0.57。原决策简报、专家问策、业务源表和学校原始文件均未改动；未提交Git，外部技术方案目录仍待仓库版本确认后同步。既有两项决策黄金样例失败和原大屏端到端超时仍保留为全项目遗留项。

10. 新增“本次重点数字”：1680px宽屏在分析右侧，普通窗口放在结论上方，可收起。只读取本次结果已有字段或同表汇总，不增加另一套评分；无数据不填0。可用卡片跳到对应明细，没有对应表不提供跳转；条件变更后显示“上次分析数字”并禁用跳转。保研资料不足时不造资格数字。

最终复验：49项后端专项测试、13项数字展示检查、25场景真实数据只读检查、两组专家团接口检查均通过。课程名称修复后的服务再次通过接口检查，浏览器确认名称及56门/18门结果。最终前端生产构建通过（2565模块）；全项目严格类型检查仍退出2，本轮专家团文件诊断为0。浏览器实测1680px侧栏、980px顶部数字区、数字跳转和条件变更禁用，已恢复默认窗口；控制台未见本次操作错误。未把全项目或全部专家正式能力标记完成。

后续仍需完成：已批准模型服务下的开放式对话与更完整的领导讨论交互、年度推免/转专业与完整毕业学位规则、领导试用和全项目回归。当前“保研”主要提供条件与资料边界，不能以五个入口已存在代替五位专家全部完成。

### 2026-09-13 续建轮次（本轮范围已实现，专项验收通过）

- EXP-06：专业课程分层、原文主要课程和修读要求对照；组合必修单列，不按逐门必修累加。
- EXP-07：转专业方案差异的学期安排、学分完整性，以及权限内单个学生的有效修读记录对照。不给认定、录取或开课承诺。
- EXP-08：同一讨论内调整比较口径、对象和场景并重新计算；条件变更提醒、可编辑讨论稿和当前浏览器草稿恢复。
- EXP-09：专项公式/权限/会话测试、实际数据检查、前端构建、接口与浏览器回归、同步口径。

涉及独立 expert_team 后端、独立专家团页面和类型、专项检查及本记录/82/指标确认书；不改旧决策模块，不修改业务源表，不扩大学院权限。验收以真实计算、缺失边界、连续讨论不丢失及越权拒绝为准，不以新增入口数衡量完成。

本轮结果：

1. 版本升级为 `expert-team/1.2`。新增 `curriculum.py`、`transfer.py`、`conversation.py` 和深度专项测试；修改独立分析、讨论接口/JSON存储及前端。沿用现有 JSON 字段，不新增数据库结构，不重跑原始数据导入。
2. 明确课程源行模块优先于 Word 展平的标题顺序，避免合并单元格造成错误跨段归类；分层不明单列。主要课程只按全名唯一对应，修读要求冲突不择一；未把原文自动扩写为先修关系或课程大纲。
3. 专业候选按当前口径计算，最多3个；只改变口径的追问保留原比较对象。2022计算机的去公共候选仍为电子信息工程30.7%、自动化25.5%、人工智能21.3%；专业基础与电子信息为3/11=27.3%。主干/实践口径可能无正重合候选，此时不凑数。
4. 转专业改按目标逐门必修计算，组合必修不整池累加；单列不完整学分及不明确学期。可选择权限内学生，复用已有有效成绩规则，显示通过/替代待确认/未通过/待明确/无结果；不将这些状态变成认定、资格、录取或开课承诺。
5. 讨论分为分析概览、数据明细和讨论记录；同一讨论可改条件重算并保留前后摘要。条件变更提醒、编辑讨论稿、版本冲突保护与浏览器未发送内容恢复已接入；已有意见稿不自动改写，下载前重新向服务端核对权限及版本。
6. 29项专项测试通过，包括必修池、原文模块要求学分、分层、学分、学期、原文冲突、学生隔离、有效成绩、同一讨论原子更新、旧版本拒绝及追问保持比较对象。前端最终生产构建通过（2564模块）。全项目严格类型检查仍退出2，新增工作区无诊断；不把旧项目问题标为解决。
7. 最新服务上的 `e2e_expert_team.py`通过：5专家目录、教务处53份/学院6份方案、角色及学院/学生越权、讨论归属、草稿保留、场景接续、原子重算和过期版本冲突。脚本创建明确标注的测试讨论，不更改教学事实。
8. 浏览器已验证最新实际页面：追问只看主干保持电子信息比较对象，条件修改阻止混用旧结果，前后讨论保留，生成意见稿可编辑保存，刷新后从最近讨论恢复未发送问题且已存意见未被覆盖。原决策简报、专家问策入口均保留。浏览器验证不代替全角色回归。
9. 本机执行与浏览器通道已恢复可用；处理了当前 PowerShell 会话的历史记录访问问题，核对并仅重启本项目后端，继续使用隐藏后台机制，前端服务未中断。沙箱助手启动/审批曾出现延迟，日志不足以证明永久根因；未修改全局安全设置、扩大目录权限或终止无关应用。
10. 本轮更新仓库指标确认书 V0.56、计划和本记录；未同步外部技术方案目录、未提交 Git。原2项决策黄金样例失败、原大屏端到端超时是既有整体门禁问题，本轮未改其数据或测试规避。
11. 最后一次原文对账发现大学英语3门均标“必修”、各4学分，但模块只要求4学分。新增精确区块核对，超过模块要求的必修池单列，不能整池累计为学生义务；每行附单列原因。测试覆盖原文区块不完整、多个要求值、以及同一课程在别处另有独立必修要求，未从近似名称推导选课规则。
12. 本轮桌面1440px与窄窗口980px检查未发现页面横向溢出；窄屏导航转为分组横向排列，测试后恢复默认窗口尺寸。浏览器确认原文主要课程、修读要求与学生逐课成绩来源均可阅读；个人对照测试记录已标为非正式意见。

| 编号 | 内容 | 状态 | 验收条件 |
|---|---|---|---|
| EXP-00 | 现状确认与独立边界 | 已确认 | 新旧专家目录分离；保留旧路径与算法 |
| EXP-01 | 原文及源课程整理、独立存储、权限 | 首期专项通过 | 可重复导入，不改源文件和旧业务表；按对象披露覆盖与冲突 |
| EXP-02 | 专业比较与培养衔接 | 首期专项通过 | 同年级/类型候选，专业变体去重；分子分母与课程明细可对账 |
| EXP-03 | 五专家交互、讨论与依据 | 首期交互验证通过 | 对象选择、按场景分析、结构化追问、草稿、历史与身份隔离 |
| EXP-04 | 毕业准备、课程表现、规则缺失状态 | 已实现首期范围 | 复用真实事实，未接入规则明确不可判断 |
| EXP-05 | 回归、口径、部署与交接 | 专项通过，整体门禁未全通过 | 专项测试、前端构建、浏览器及权限反向验证；整体遗留问题见下文 |

## 首期不宣称实现

未取得确认规则前的转专业资格/录取、推免资格和正式排名、完整毕业与学位审核；无大纲时的教学内容等价；无支撑关系时的能力达成；无未来安排时的开课承诺；无可比观测时的建设效果归因。

## 数据检查基线

V2中53份2022级方案、13863条方案课程；39份有已入库目标与毕业要求。284个课程代码的4508条方案行缺课程主数据关联，但其名称、学时和考核方式已在原始方案课程文件中找到。52份方案存在唯一的学生专业代码候选，候选映射不得当作正式主数据覆盖。课程支撑和毕业指标点表为空，需重新盘点原文。人工智能方案已入库目标含石油工程表述，需保留冲突并追溯。

## 验收记录

1. 只读 Word 提取 39 份，全部精确匹配方案名称；源课程表整理 13863 行。原文课程名称补齐不覆盖 V2 课程主数据。
2. 地球物理方案存在“一 、”带空格章节号，已修正分章规则。人工智能方案原文的培养目标确为石油工程内容，保留冲突，不用于定位判断。
3. 源表“课程类别”实际是本科/预科等学历层次，不能据此识别通识课程。算法优先读取原文的课程模块层级；原文缺失时使用版本化的2022级通识模块名称目录，混合与未明确归类保留。不是语义相似度。
4. 首次生产构建通过。完整后端测试运行476项，474通过、2项失败：原有 `test_decision_golden` 的 `sig-ap-stale` 和固定数字950不匹配。本次未修改其信号算法、黄金样例或数据以消除失败。
5. 全项目 TypeScript 严格检查输出322行诊断、退出码2；新 `expert-team` / `expertTeam` 文件诊断为0。不能据此宣称全项目类型检查通过。
6. 最终前端生产构建通过（2564模块），`git diff --check`通过。新增13项专项测试全部通过，25个场景实际数据只读检查均正常返回结果或明确的限制状态；这不等于25个场景都具备完整业务判断能力。
7. `e2e_expert_team.py`通过：无令牌、非领导身份、学院源方案与目标方案越权、跨身份讨论读取、追问、草稿保存和过期版本保存冲突。教务处53份方案、学院测试身份6份；计算机专业三个候选为电子信息工程、自动化、人工智能，结构重合分别31/101=30.7%、27/106=25.5%、20/94=21.3%。
8. 浏览器已验证实际入口、1440px桌面分栏及窄面板显示、方案搜索选择、比较返回、原文章节展开、口径追问和讨论稿编辑保存；未把接口权限验证表述为全部角色均完成浏览器走查。账号名`admin`实际可带教务处身份，准入按当前工作身份判断，不按账号名称判断。
9. 旧 `e2e_check.py`的健康、登录和菜单检查通过，随后在原大屏接口达到30秒超时而中止，整套端到端检查未通过。保留原脚本与模块，不扩大本次修改范围。
10. 页面验收及接口联调产生少量明确标注为测试的讨论记录，存于独立新库；未删除用户内容，未提交Git。外部技术方案目录的确认书待仓库版本确认后再同步，本次更新的是仓库版本。

## 实际首期能力，不以25个场景入口代替完成承诺

- 专业建设：最多三个近邻、双方课程覆盖、共同/独有明细、学分学期对照、原文定位摘录、专业内部安排，以及附条件的调整讨论方向。不能判定语义等价、特色充分性或撤并必要性。
- 转专业：独立的目标必修覆盖排序、目标新增必修清单、授权群体历史异动。无当年规则与学生有效认定时，不给申请资格、认定学分、补修承诺或接收名单。
- 课程质量建设：当前方案群体的首次修读记录与单课跨学期观察。大纲、考核、建设措施未接入，暂不做原因归属、建设方案效果测算。
- 毕业资格审核：适用关系、课程缺口与资料问题分开；提取原文修读要求的最低总学分、必修、选修和单设实践学分并保留来源，不相加代替总要求。读取既有方案进度规则结果，不作毕业或学位认定；无快照则不做前后变化。
- 保研：目前提供年度规则与资料缺口说明，尚不具备资格计算、正式排名及录取预测。
- 对话：当前模型配置关闭且未配置可调用模型。新工作区只实现可追溯的项目说明、计算口径、资料限制、意见草稿及会话保存，明确提示不支持的问题；没有擅自接入外部模型或发送学校数据。

## 部署与恢复

```powershell
# 在 code 目录下，使用项目 Python 环境
python -X utf8 scripts/prepare_expert_team.py --documents ../work/expert-team/documents.json
python -X utf8 -m unittest backend.tests.test_expert_team -v
python -X utf8 scripts/check_expert_team.py
```

新数据与讨论保存在 `code/backend/db/expert_team.sqlite`。第一次迁移前使用 SQLite backup 备份旧库为 `analytics.before-expert-team.sqlite`。旧库只新增专家团菜单、两个领导角色的菜单及动作授权；旧业务表不更新。迁移可重复执行，不覆盖首次备份。新菜单排在现有两个入口后。

原文提取使用 `code/scripts/extract_expert_documents.ps1`：宏禁用、只读打开、不保存源文件，结果写到本项目。Word 在受限登录会话下不能创建实例时，需要使用当前本机登录环境执行只读提取。

入口：`http://localhost:3006/#/admin/reports/expert-team`。需要教务处或学院院长当前身份，授权在服务端再次校验；系统管理员、校领导及其他角色不因拥有其他AI菜单而自动获得新专家团权限。

讨论按账号、工作身份及范围指纹隔离；再次读取时检查方案与全部候选是否仍在范围内；保存有版本冲突保护。身份变化清空前端缓存并丢弃旧请求响应。新接口读、分析与保存复用审计记录，不记录问题正文及敏感数据。

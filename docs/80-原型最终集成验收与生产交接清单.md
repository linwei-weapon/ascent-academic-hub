# 原型最终集成验收与生产交接清单

> 验收日期：2026-07-19
> Git 分支：`产品终稿`
> 定位：生产系统开发前的真实数据消费、指标验证与交互原型，不承担教务主数据维护。

## 1. 本轮最终结论

- 三个一级分组、16个二级入口已完成；教学管理分析、AI管理决策和系统管理职责清晰。
- 前端生产构建通过；核心页面浏览器回归无控制台错误、无错误提示、无残留加载遮罩。
- 端到端脚本覆盖鉴权、数据范围、指标公式、下钻、预警治理、V2教学与师资等契约。
- 相邻学期画像现包含“改善、稳定、恶化、变化分化、数据不足”五类；最终核对已把“变化分化”纳入分类合计和下钻验证。

## 2. 已验收的关键用户路径

1. 数据大屏：全校—学院—专业—课程—学生名单—学生详情，平均分为学分加权口径并补充 GPA。
2. 学业预警：真实学校数据—顶层卡片即时筛选—我的待办—学生详情—历史预警—跟进与状态；规则配置和规则自发现同一工作页。
3. 教学运行：课程 TOP 与完整目录、实际教室占用、晚间开关、调课原因分类与教师 TOP10、教师负荷 TOP10、重点课程排课分析。
4. 培养质量：学院—年级—专业—方案联动、毕业要求与执行证据、模块完成规则、学生进度详情抽屉。
5. 师资保障：可评估、优先核查、连续单点、结构待核实和数据候选五类管理指标；学院课程保障工作台、最近实际开课与团队证据抽屉、异常任务排除和课程责任组织边界。年龄、临退休、正式团队与未来计划未接入时不形成正式结论。
6. 学生学业：GPA 与挂科联合迁移、历史挂科模式、学生核查抽屉、统一成长轨迹。
7. 学生档案建议：按五类受众提供确定性建议；历史高挂科率课程明确提示学习难度并披露依据。
8. AI管理决策：管理要情突出变化事件；决策研判通过三个管理专家、自然语言和结构化参数输出证据卡与候选决策单。
9. 系统管理：账号、角色与功能、菜单、数据权限、指标口径、分析方案、审计和系统参数八个模块。

## 3. 生产系统必须替换或补齐的能力

| 领域 | 原型做法 | 生产要求 |
|---|---|---|
| 培养方案 | Word 表格临时解析 | 教务 API 提供版本化方案、模块、课程组、最低学分/门数、替代和毕业要求结构 |
| 学生选课 | 以成绩、认定和方案推导当前未完成 | 接入选课轮次、退补选、应选未选、教学班容量和重修班资源 |
| 成绩口径 | 已区分真实记录并提供首次/最终通过等分析 | 学校签版补考、重修、缓考、缺考、替代、最终有效成绩规则 |
| 教室资源 | 实际占用记录用于时序与楼宇分析 | 接入全部占用行为、标准节次、可排除事件类型和教室状态；平台不维护主数据 |
| 师资档案 | 教学任务、课程团队和已有职称信息 | 接入完整出生日期、职称历史、在岗状态、所属组织历史和团队责任角色 |
| 预警处置 | 原型已有责任人、跟进和受控状态 | 是否建设完整帮扶闭环、消息推送和批量任务由产品决策后立项 |
| AI建议 | 确定性指标为主体，少量模型样例并保留来源 | 明确部署方式、数据范围、留存、事实约束、人工确认和离线评估后启用模型 |

## 4. 交付前检查命令

```powershell
# 后端运行后执行接口与数据契约核对
python -X utf8 code/scripts/e2e_check.py

# 后端完整测试
python -X utf8 -m unittest discover -s code/backend/tests -p "test_*.py"

# 前端生产构建
cd code/frontend
npm.cmd run build
```

验收时还应使用教务处、学院、辅导员、教师等账号分别检查菜单准入和学生数据范围。

## 5. 产品边界

- 平台只消费和挖掘数据，不维护教室、培养方案、人员关系等学校业务主数据。
- 不扩展为学生工作管理系统，不自动执行预警处置、排课、开班或教师评价。
- 考风考纪和出勤等无可靠学校数据的领域不作为当前产品承诺。
- 统一身份认证、学校 API、真实模型接入和生产数据库在部署实施阶段完成。

## 6. 交接完成定义

后续开发团队接收时，应同时取得代码基线、数据字典、API 契约、ETL 映射、指标口径、权限矩阵、容量部署方案和本验收清单。任何指标上线前必须能回答：数据从哪里来、如何计算、谁能看到、如何下钻核查、数据不足时如何降级。

## 7. 教学数据总览07.26修改增量验收

- [x] 修改前已创建Git备份分支和仓库外bundle备份。
- [x] 历年指标弹窗在学校、学院、专业复用同一指标ID、公式和状态契约。
- [x] 历史学籍只读逐学期源库；历史预警证据不足时显示不可用，不伪造历史值。
- [x] 专业预警名单、年级更多课程只使用现有可验证字段和接口能力。
- [x] 课程未通过率使用最终有效学生口径，课程平均GPA使用最终有效课程绩点算术平均。
- [x] 课程行政班展示全部结果并分页；课程学生画像锁定课程和学期。
- [x] 学生证据和完整档案统一使用课程最新真实有效结果的学分加权总GPA。
- [x] 本轮新增页面复用系统原有学期筛选元数据的 `value/label` 与“最新在前”顺序，不单建格式或排序规则；当前数据下显示为 `YYYY-YYYY-N`。
- [x] 本轮没有新增、删除或修改数据库表、字段、索引和约束。
- [x] 使用校领导/教务处、学院、两个班主任或辅导员、两个导师、系统管理员完成最终浏览器身份回归。
- [x] 后端服务启动后执行 `code/scripts/e2e_check.py`，214项检查全部通过。

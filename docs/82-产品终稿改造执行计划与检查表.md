# 产品终稿改造执行计划与检查表

> 分支：`产品终稿`  
> 基线：`b72c29b feat:ai-management-v1`  
> 状态：P1—P6 已完成；BR-01 当前阶段实现已完成，2026-08-09 已获用户授权提交并合并
> 范围：两级菜单、预设分析页面重组、AI管理决策重构、基础报表、统一系统管理、精细数据权限
> 执行原则：上一阶段未形成验收证据，不进入下一阶段

## 1. 已确认的最终决策

- [x] 一级菜单采用“教学管理分析、AI管理决策、基础报表、系统管理”，顺序固定。
- [x] 决策模拟并入“决策研判”，不单独作为二级菜单。
- [x] 五个事实型管理专题归回对应业务分析模块，不重复维护指标。
- [x] 普通用户不接触 Skill 概念；系统管理使用“分析方案管理”。
- [x] 系统管理员默认拥有全校业务数据权限。
- [x] 一人多角色采用“当前工作身份”切换。
- [x] 预设业务页面负责事实分析，管理专家负责优先级、解释和模拟。

## 2. 完成定义

整个“产品终稿”只有同时满足以下条件才算完成：

1. 菜单、路由、角色权限和默认入口一致，没有孤立或点击无响应页面。
2. 所有业务接口使用统一权限上下文，前端隐藏不能替代后端鉴权。
3. 六个预设分析模块无重复专题入口，相同指标调用同一计算口径。
4. 管理要情以变化事件为主，决策研判支持卡片化对话和情景模拟。
5. 学校个性化管理方法能够以分析方案/管理专家配置承载，不复制整套页面。
6. 校、院、班级、导师和系统管理员数据范围均通过正向和反向测试。
7. 前端构建、后端测试、端到端检查、浏览器回归和 `git diff --check` 全部通过。
8. 文档、数据库迁移、API契约、权限矩阵和验收证据同步更新。
9. 每个教学管理分析模块均已同步更新《本科教学分析与学业决策支持平台需求调研及指标口径确认书V2》，且指标名称、计算逻辑、管理用途、数据来源、边界和核查路径与系统实现一致。

## 3. 阶段总览

| 阶段 | 目标 | 主要产物 | 门禁 |
|---|---|---|---|
| P0 | 建立分支、计划和执行上下文 | Git分支、AGENTS、检查表 | 基线可追溯 |
| P1 | 建立两级菜单与信息架构 | 菜单迁移、树形导航、路由兼容 | 多角色菜单回归通过 |
| P2 | 建立统一数据权限底座 | 权限上下文、关系模型、管理页面 | 越权反向测试通过 |
| P3 | 重组预设业务分析模块 | 六模块归位、角色视角、专题迁移 | 无重复口径和断链 |
| P4 | 重构AI管理决策 | 管理专家、管理要情、决策研判 | 可追溯、可调整、可模拟 |
| P5 | 收口系统管理 | 账号、角色、数据、菜单、指标、方案、审计、参数 | 管理配置可用 |
| P6 | 全量验收与交接 | 自动化、浏览器证据、文档、Git提交 | 全部完成定义满足 |
| BR-01 | 九张基础报表 | 只读查询、Excel、四级菜单、权限、迁移、口径登记 | 自动化通过后人工验收；未授权不提交/合并 |

---

# P0 基线与执行准备

## P0-01 Git基线

- [x] 确认来源分支为 `AI能力分支`。
- [x] 确认基线提交为 `b72c29b`。
- [x] 创建本地分支 `产品终稿`。
- [x] 推送 `origin/产品终稿` 并设置上游。
- [x] 保留工作区未跟踪的 `work/` 和临时文件，不擅自删除或提交。

验收证据：

- `git branch --show-current` 返回 `产品终稿`。
- `git status --short --branch` 显示跟踪 `origin/产品终稿`。

## P0-02 执行上下文

- [x] 新建根目录 `AGENTS.md`。
- [x] 记录已确认的产品决策和不可改变边界。
- [x] 记录按任务读取的最小文件集合。
- [x] 记录启动、测试、Git和子任务交接规则。
- [x] 建立本执行计划与检查表。

阶段门禁：

- [x] 两份文件经过内容检查。
- [x] `git diff --check` 通过。

---

# P1 两级菜单与信息架构

## P1-01 菜单真值源与迁移

- [x] 盘点 `sys_menu`、`sys_role_menu` 当前数据和角色授权。
- [x] 原阶段新建三个一级父菜单；BR-01 在不改变既有模块归属的前提下新增第四个一级父菜单：
  - [x] 教学管理分析。
  - [x] AI管理决策。
  - [x] 基础报表（BR-01新增）。
  - [x] 系统管理。
- [x] 为现有叶子菜单设置正确 `parent_id`。
- [x] “决策模拟”菜单名称移除，既有能力以“决策研判”入口承载，兼容路由保留。
- [x] 原五个事实型专题不再作为AI决策菜单叶子项。
- [x] 编写幂等菜单迁移脚本。
- [x] 迁移脚本可重复执行且不会重复创建或破坏角色授权。
- [x] 记录迁移前后菜单快照。

验收：

- [x] BR-01 后数据库目标结构为四类一级菜单，基础报表下只挂九个报表叶子。
- [x] 每个二级菜单只有一个父级。
- [x] 没有空父菜单、孤儿菜单或重复路径。

## P1-02 登录菜单契约

- [x] 登录与 `/auth/me` 返回 `parent_id`。
- [x] 返回父菜单和已授权叶子菜单。
- [x] 角色没有任何子菜单权限时不返回空父菜单。
- [x] 父菜单不作为业务数据接口权限凭据。
- [x] 更新菜单接口契约和测试。

## P1-03 侧栏交互

- [x] 侧栏按 `parent_id` 构建真实两级树。
- [x] 默认只展开当前路由所属一级菜单。
- [x] 点击父菜单只展开/收起，不进入空页面。
- [x] 当前产品不设置侧栏整体折叠入口，避免出现只有图标、含义不清的一级分组。
- [x] 下钻到学院、专业、课程、学生时保持正确二级菜单归属。
- [x] 页面刷新后保留当前路径，并恢复所属一级菜单展开状态。
- [x] 单身份登录、刷新后菜单立即按当前授权刷新；一人多身份切换统一在 P2 实现。

## P1-04 路由与兼容

- [x] 路由守卫以叶子菜单校验页面准入；页面动作权限统一在 P2 建模。
- [x] 学生详情允许从预警、学生分析和合法证据链接进入。
- [x] 原专题 URL 继续可访问，并在导航权限上归回对应业务模块。
- [x] 菜单迁移不重写业务 URL，既有返回路径和筛选参数不丢失。
- [x] 无权限访问进入明确的 403 说明页，不再静默跳到无关模块。

## P1-05 菜单和角色管理

- [x] 菜单管理改为树形列表。
- [x] 新建/编辑菜单可选择父菜单。
- [x] 父菜单不能选择自身、二级菜单或自己的后代。
- [x] 角色菜单分配改为真实树形勾选。
- [x] 勾选子菜单时父菜单呈现半选/选中状态。
- [x] 保存时以后端叶子权限为准，避免仅勾选父菜单获得全部权限。

## P1-06 角色默认入口

- [x] 校领导、教务处：优先进入教学数据总览。
- [x] 二级学院：进入本学院教学数据总览。
- [x] 辅导员：进入学业预警监控；班主任身份和带班关系在 P2 接入。
- [x] 导师默认入口规则已预留，导师身份和带生关系在 P2 接入。
- [x] 系统管理员：无指定来源时默认进入账号管理，同时可以访问全校业务分析。
- [x] 默认入口无权限时回退到该身份第一个合法叶子菜单。

P1阶段门禁：

- [x] 前端生产构建通过。
- [x] 菜单接口与完整后端测试通过。
- [x] 当前原型已有角色的接口自动化证据齐全，并完成管理员、学院、辅导员浏览器回归。
- [x] 所有旧链接兼容映射验证通过。

P1 验收记录见 `docs/83-P1两级菜单与信息架构验收记录.md`。

---

# P2 统一数据权限底座

## P2-01 权限现状审计

- [x] 列出旧库 `sys_role_scope` 使用位置。
- [x] 列出V2 `access_scope_mapping` 使用位置。
- [x] 列出AI模块内独立角色白名单和范围函数。
- [x] 列出班主任、导师和现有 `staff_student_scope` 数据。
- [x] 确认辅导员—行政班数据表的字段、主键、有效期和来源。
- [x] 形成“保留、迁移、废弃”清单。

P2-01审计结果和统一模型设计见`docs/84-P2权限现状审计与统一模型设计.md`。

## P2-02 统一权限上下文

定义统一返回结构：

```json
{
  "userId": "",
  "staffId": "",
  "activeRole": "",
  "menuPermissions": [],
  "actionPermissions": [],
  "detailScope": {},
  "comparisonScope": {},
  "fieldPolicy": {},
  "scopeFingerprint": ""
}
```

检查项：

- [x] 当前工作身份显式进入权限上下文。
- [x] 角色决定能力上限，具体用户和业务关系决定数据对象。
- [x] 权限缺失默认拒绝，不回退到全校。
- [x] 系统管理员作为明确授权角色返回全校范围。
- [x] 权限上下文可供旧库、V2、AI、导出和审计复用。
- [x] `scopeFingerprint` 可用于缓存隔离和会话失效。

## P2-03 数据关系模型

- [x] 支持用户多角色绑定及生效时间。
- [x] 支持用户/教职工与学院组织范围绑定。
- [x] 建立辅导员、班主任—行政班关系模型（辅导员真实源表待学校补充）。
- [x] 复用或规范导师—学生关系。
- [x] 记录关系类型、生效时间、失效时间、来源和状态。
- [x] 支持转班、换导师、调岗后的当前权限变化。
- [x] 保留历史关系用于审计，不用于当前越权访问。
- [x] 提供幂等迁移与ETL。

P2-02/P2-03实施记录见`docs/85-P2统一权限上下文实施记录.md`。

## P2-04 不同业务对象的“本学院”定义

- [x] 学生、成绩、预警：学生所属学院。
- [x] 培养方案：方案所属专业和学院。
- [x] 教学运行：教学任务或课程开课学院。
- [x] 师资保障：教师组织和课程开课组织。
- [x] 跨学院课程：本院学生明细与全课程聚合分离。
- [x] 将上述规则写入权限契约和API测试。

## P2-05 跨学院聚合比较

- [x] 学院角色可查看允许的其他学院聚合指标。
- [x] 其他学院聚合行不返回学生明细标识。
- [x] 其他学院行没有学生名单和档案下钻。
- [x] 自己学院行可以按授权继续下钻。
- [x] 小样本或可反向识别场景按学校策略隐藏或合并。
- [x] 聚合接口和明细接口使用不同权限声明。

## P2-06 各模块接口接入统一权限

- [x] 教学数据总览与五级下钻。
- [x] 学业预警、规则证据、历史预警。
- [x] 教学运行和课程明细。
- [x] 培养质量、方案执行和学生进度。
- [x] 师资保障、教师档案和课程团队。
- [x] 学生成长分析、名单和学生档案。
- [x] 管理要情、决策研判和模拟。
- [x] 导出和下载。
- [x] 审计与系统配置。

要求：

- [x] 删除或迁移各 router 新增的重复角色白名单。
- [x] 直接传入越权学院、学生、课程或教师ID时服务端拒绝。
- [x] 无映射范围返回403或空授权结果，不泄露全校统计。

## P2-07 当前工作身份

- [x] 多角色用户可查看合法身份列表。
- [x] 右上角展示当前身份和当前数据范围。
- [x] 切换身份重新获取菜单和权限上下文。
- [x] 切换后清理不适用筛选、缓存和AI会话。
- [x] 所有访问日志记录当时的工作身份。

## P2-08 数据权限管理页面

- [x] 组织范围配置。
- [x] 辅导员/班主任带班关系。
- [x] 导师带学生关系。
- [x] 生效和失效时间。
- [x] 数据来源和最后同步时间。
- [x] 未映射、重复、过期和冲突关系提示。
- [x] “以此用户核验权限”预览。
- [x] 显示菜单、数据范围、可见学生数量和关键操作权限。

P2阶段门禁：

- [x] 校领导和教务处全校范围通过。
- [x] 系统管理员全校业务范围通过。
- [x] 两个学院之间明细隔离通过。
- [x] 两个辅导员/班主任之间班级隔离通过。
- [x] 两个导师之间学生隔离通过。
- [x] 跨学院聚合可见、明细不可见通过。
- [x] URL、API、导出、AI、证据链接反向测试通过。

P2权限契约、管理页面和验收证据见`docs/86-P2数据权限契约与验收记录.md`。

---

# P3 预设教学管理分析模块重组

## P3-01 统一页面配置边界

- [x] 固定页面负责事实、指标、趋势、表格和下钻。
- [x] 学校可配置标题、指标显隐、排序、阈值说明和默认筛选。
- [x] 复杂计算仍由统一指标服务提供，不由前端或提示词临时计算。
- [x] 页面明确显示当前工作身份、数据范围、学期和数据时间。
- [x] 加载、无数据、权限不足和部分数据状态具有统一提示。

## P3-02 教学数据总览

- [x] 菜单和标题统一。
- [x] 校级、院级、班级范围使用同一指标口径。
- [x] 校领导、教务处支持全校下钻。
- [x] 学院支持本院下钻和其他学院聚合比较。
- [x] 辅导员、班主任只显示所带班级范围。
- [x] 加权平均分、GPA、未通过、预警等指标来源和公式可追溯。
- [x] 学校、学院、专业共用历年指标下钻，分子、分母、来源状态和不可用原因可核查。
- [x] 专业预警名单和年级更多课程仅使用现有可验证字段，均由服务端按身份范围过滤。
- [x] 课程学生画像锁定课程与学期，课程成绩/绩点、学生证据和完整档案返回链保持上下文。
- [x] 课程未通过率、课程平均GPA和学生总GPA已统一为最新真实有效结果口径。
- [x] 07.26修改未增加、删除或变更数据库字段及其他数据库结构。

## P3-03 学业预警监控

- [x] 预警监控、规则配置和规则自发现仍在同一工作区。
- [x] 规则自发现运行前展示真实样本库表清单与脱敏说明，强制协议确认；执行改为可追踪后台任务，新一轮启动时保留旧建议并标记为已替代。
- [x] 规则变更单草稿支持维护普通 SQL 或存储过程试算脚本；保存后旧试算失效，权限、状态和脚本非空由服务端校验。
- [x] 当前风险、历史变化和学生档案范围一致。
- [x] 辅导员、班主任按行政班关系过滤。
- [x] 导师如开放预警入口，只能查看所带学生。
- [x] 顶部筛选交互不整页刷新，状态变化有明确反馈。
- [x] 查询条件增加年级，学院、专业、年级、班级选项按当前身份授权范围实时级联；重新打开学院或专业下拉框仍保留同层可切换选项。
- [x] 四个学生KPI仅定位并筛选学生名单，不改变其他KPI；真实全校数据已逐卡对账为3603、1790、1116、2163。
- [x] 当前预警时间分布按最近10个有数据月份展示，完整显示“学生数”纵轴名称，并在标题处显示最新预警统计日期；两张图表卡片高度增至原来的1.5倍。
- [x] 当前预警学生率按当前权限及查询条件范围内在籍学生作分母；组织图范围平均标签横向显示在竖线顶部且不与首条柱形重叠。
- [x] 当前预警学生名单在专业列后默认显示班级和年级。
- [x] 低年级风险观察按学院、专业、年级、班级提供授权级联查询；学院约束专业，专业和年级共同约束班级；五张指标卡定位并筛选学生核查名单，名单总数与卡片对账一致。
- [x] 低年级关注优先级随学院、专业、班级逐级切换分组和标题，并按大一未通过学生比例降序。
- [x] 低年级学生名单补充学院和班级，核查抽屉使用真实学期代码展示GPA轨迹并移除重复提示文案。
- [x] 低年级学生核查名单移除重复边界标签，增加学号/姓名片段模糊查询；查询只影响名单与分页，名单重置保留当前观察条件和名单预设，接口继续执行身份范围校验。
- [x] 预警规则审核职责和入口已核对：质量办账号显示待审核记录的“通过、拒绝”，教务处负责创建/发布、校领导负责激活，服务端执行最终权限与创建人隔离校验。
- [x] 规则候选学生明细移除变化类型分类切换和变化列，统一分页展示当前变更单全部候选学生；治理内部变化类型继续用于汇总、激活、导出和审计。
- [x] 规则变更影响分析将学院、专业、年级三个页签合并为交叉分布，按实际年级动态列、学院小计和全校合计展示全部候选影响，并与候选名单总数对账。
- [x] 规则变更影响分析移除交叉表标题下方重复统计范围说明，统计逻辑和口径保持不变。
- [x] 指标口径确认书已同步至V0.25，现行完整页面与下钻说明见`docs/108-学业预警监控各模块功能及指标点详细需求说明.md`；107文档保留为历史演进记录。

## P3-04 教学运行分析

- [x] 接收课程质量与教学运行专题的事实分析。
- [x] 接收排课策略与资源优化专题的事实分析。
- [x] 课程、教室、调课、负荷、排课各Tab共享范围和学期上下文。
- [x] 学院详细数据按开课组织授权。
- [x] 课程完整清单、重点TOP和证据下钻无冲突。

## P3-05 培养质量分析

- [x] 接收毕业准备核查与课程保障专题的事实分析。
- [x] 学院—年级—专业—方案联动保留。
- [x] 方案模块、学分/门数规则、学生绑定和执行进度使用统一事实。
- [x] 学院关注学生可以进入当前上下文详情抽屉。
- [x] 不将毕业要求原文包装成过重的达成度结论。
- [x] 四个工作区统一为方案执行总览、方案结构与要求、学生进度核查、毕业准备与课程保障。
- [x] 方案源未覆盖年级不计为绑定异常；年级或专业错配进入绑定待核验且不参与正式进度。
- [x] 学生进度不再以全部课程池作为分母，模块评价按可信学分、门数/选择组、逐门必修顺序执行。
- [x] Word合并单元格导致的父级学分误继承经过课程池上限校验，不把不可能的规则用于学生结论。
- [x] 当前学期不提前计为逾期；缺少选课、缓修、免修和认定证据时只形成数据候选。
- [x] 单一教师、无替代和历史无开课不单独触发课程保障风险；未来任务未接入时显示待确认。
- [x] 管理总览、方案详情、进度和毕业准备按需加载，正式名单真实分页，抽屉即时显示加载状态。
- [x] 正式表格支持显示列、列顺序、显示上限和密度；旧达成度路由兼容重定向。
- [x] 指标口径确认书专题四同步至V0.5，详细依据见`docs/97-培养质量分析UE深度走查与优化方案.md`。

## P3-06 师资保障分析

- [x] 模块定位为课程供给连续性、团队结构证据和数据可信度核查，不评价教师个人；教师负荷与历史排课行为继续归属教学运行分析。
- [x] 顶部指标统一为可评估课程、优先核查课程、连续单点课程、结构待核实课程和数据候选课程，均说明管理用途、触发条件与边界。
- [x] “一门课一位教师”只作为事实，不自动触发风险；优先核查同时要求重点保障属性、规模门槛和连续性或集中度证据。
- [x] 最近3次实际开课用于连续单点判断；职称结构规则要求团队规模和证据完整率，未接入年龄、临退休、正式团队与未来计划时不形成结论。
- [x] 已登记高等级异常教师任务从正式指标排除，受影响课程进入数据候选并披露排除记录与影响课程数。
- [x] 课程责任组织按课程主数据中的责任院系执行，教师人事归属只作为成员证据；学院级直接进入本院工作台。
- [x] 课程与教师详情在当前业务上下文抽屉展示，旧路由兼容重定向，不跳离模块。
- [x] 表格支持显示列、列顺序、密度和显示上限；总览预热和短时缓存避免重复长时间计算。
- [x] 指标口径确认书专题九同步至V0.6，详细依据见`docs/98-师资保障分析UE深度走查与优化方案.md`。

## P3-07 学生成长与学业分析

- [x] 接收低年级受挫的学生事实和成长分析。
- [x] GPA与挂科联合迁移。
- [x] 成长轨迹包含成绩、挂科、培养进度、历史预警和必要干预记录。
- [x] 辅导员、班主任和导师使用同一事实、不同表达重点。
- [x] 学生详情使用上下文抽屉或返回原筛选位置。

2026-07-24 深度 UE 重构进度（见 `docs/99-学生成长与学业分析UE深度走查与优化方案.md`）：

- [x] 阶段 A：统一学分加权 GPA、本学期未通过学生率、课程有效结果和按课程去重学分；284 项后端测试、生产构建及三类身份浏览器回归通过。
- [x] 阶段 B：收口为“学生成长与学业分析”单一菜单；按校级/学院、辅导员/班主任、导师身份加载对应工作区；旧 URL 兼容重定向；288 项后端测试、190 项端到端检查、生产构建及五类身份浏览器回归通过。
- [x] 阶段 C：重构管理首屏、组织比较、关注分组和学生名单；五项管理指标、四类重叠关注组、组织下钻和可解释证据名单已通过 203 项端到端检查及校级/学院浏览器回归。
- [x] 阶段 D：统一学生学业证据抽屉；公共抽屉、V2方案进度证据、12项专项检查、生产构建及208项端到端检查完成，并通过校级、学院、辅导员、班主任、导师五类身份浏览器门禁。
- [x] 阶段 E：概览/组织/名单/证据接口拆分、学生学期与课程有效结果汇总、缓存、URL状态、局部重试和返回恢复完成；全校冷请求0.86—1.65秒、缓存约15毫秒，297项后端测试、210项端到端检查、生产构建、五类身份浏览器回归和外部V0.7口径确认书同步通过，模块已完成Git收口。

## P3-08 清理重复专题

- [x] 原专题路由映射到新归属或兼容说明。
- [x] 删除重复指标请求和重复口径描述。
- [x] AI管理要情和管理专家改为引用业务模块证据。
- [x] 文档、菜单、页面标题和面包屑同步更新。

P3阶段门禁：

- [x] 六模块逐页业务验收通过。
- [x] 没有重复菜单和点击无响应入口。
- [x] 相同指标在不同页面的值、公式、范围一致。
- [x] 所有详情下钻和返回路径通过。

---

# P4 AI管理决策重构

## P4-01 管理专家与分析方案协议

每个专家至少包括：

- [x] 专家名称、版本、适用角色和管理问题。
- [x] 所需数据与完整性条件。
- [x] 指标、规则、阈值和排序逻辑。
- [x] 事件触发条件和不应触发条件。
- [x] 输出卡片和证据路由。
- [x] 可继续追问的意图和可调整参数。
- [x] 推荐动作、建议责任角色和时间。
- [x] 模拟器能力。
- [x] 不确定性、适用边界和测试案例。
- [x] 学校配置覆盖和版本回滚。

## P4-02 首批管理专家

- [x] 毕业准备核查专家。
- [x] 高影响课程保障专家。
- [x] 课程团队连续性专家。
- [ ] 后续迁移低年级受挫和排课资源优化专家。
- [x] 专家共享统一指标和权限上下文，不复制取数代码。

## P4-03 管理要情

- [x] 从静态日报改为管理事件流。
- [x] 识别新增、升级、扩大、缓解和退出关注。
- [x] 同根因、同对象事件聚合，避免重复。
- [x] 首屏最多显示少量高价值事件。
- [x] 每张卡片包含变化、影响、为何现在看、首要核查动作和证据。
- [x] “继续研判”自动带入事件、角色、范围、学期和专家。
- [x] 无明显变化时明确显示无新增重大事项。

## P4-04 决策研判工作区

- [x] 首页提供推荐管理问题，不以空白聊天框开始。
- [x] 对话和结构化筛选/参数双向同步。
- [x] 系统显示“我理解的分析范围和临时条件”。
- [x] 结果以事件卡、证据卡、比较卡和候选决策单呈现。
- [x] 临时会话调整不改变正式管理口径。
- [x] 保存为正式分析方案需要授权和发布流程。
- [x] 决策模拟作为研判能力嵌入，不再单独菜单。
- [x] 原三类模拟场景继续可用。

## P4-05 AI权限与追溯

- [x] 专家运行前使用统一权限上下文。
- [x] 自然语言不能扩大组织或学生范围。
- [x] 下钻证据再次鉴权。
- [x] 缓存键包含范围指纹、角色、专家版本和参数版本。
- [x] 角色切换或关系失效后旧会话失效。
- [x] 每个结果显示数据来源、时间、范围、规则版本和限制。
- [x] LLM不直接计算正式指标，不输出无证据趋势判断。

## P4-06 性能与体验

- [x] 管理要情使用快照、缓存或预计算。
- [x] 常用更新目标2秒内。
- [x] 研判参数重算目标1秒内。
- [x] 首次重型计算有明确进度和阶段提示。
- [x] 对话只局部更新相关卡片，不整页闪烁刷新。
- [x] 用户可以一键恢复正式口径或清除临时条件。

P4阶段门禁：

- [x] 三个首批专家在校级和院级范围均验证。
- [x] 每项管理结论都有明确管理意义和事实来源。
- [x] 对话调整、模拟、恢复和保存边界验证。
- [x] 越权自然语言请求被正确限制。
- [x] 浏览器交互无整页无提示刷新。

P4协议、权限、性能和浏览器验收证据见 `docs/88-P4AI管理决策重构验收记录.md`。

## P4-07 体验重构（产品终稿 R1–R6，2026-07-22 完成）

按 `docs/83-AI管理决策体验重构修改计划.md` 执行，六项用户决策落地：

- [x] AI决策不形成办理闭环：建议追踪（交办/处理中/完成/忽略/上期跟进）全部下线，接口返回 410 一个版本周期；核查清单为 AI 输出终点。
- [x] 查证窗口：GET `/signals/{id}/evidence` 完整证据包 + `/admin/verify/signal/:id` 独立只读页，新开浏览器窗口，越权一律 404。
- [x] 简报首屏 v2：Top3（第 1 条展开）+ 折叠行；同信号单页只出现一次；动作三件套「问专家/查证·新窗口/核查清单（可复制）」；冷启动分段进度。
- [x] 专家问策：4 位专家纯对话（三栏：会话栏/对话主窗/依据栏），SSE + evidence_refs [n] + 越界拒答指路 + 会话按用户隔离落库；旧追问抽屉 ChatDrawer 退役，POST `/chat` 保留一个版本周期。
- [x] 菜单收口：AI管理决策二级菜单 = 决策简报 / 专家问策，授权与简报一致（幂等迁移脚本）。
- [x] 回归：后端 158 测试 OK；e2e 188 项全 PASS；前端生产构建通过。

## P4-08 数据要素明细下钻（2026-07-22 完成）

用户决策：点数字不弹公共查证页，人数类数字直达该数字代表的业务明细清单（行动入口）；
查证页保留给「依据[n]」做信任核验。明细与查证分工：查证回答"可信吗"，明细回答"都是谁"。

- [x] Skill 明细契约：`Skill.detail_specs = {signal_type: {facts标签: 规格}}`，
  规格含 context_key/columns/total_key/filter；未声明的数字 = 聚合/判定值，前端渲染为纯文本不诱导点击。
- [x] 明细接口：GET `/admin/ai/decision/signals/{id}/detail?fact=标签`；
  明细行取自信号 context（Skill 按当前身份数据范围预计算），接口不接受客户端筛选参数；
  越权/未知信号 404，聚合值 400，行数硬上限 200（truncated 提示去专题工作区）。
- [x] 两个 Skill 落地：预警优先级（队列人数/其中严重级→queue 行含评分事由，滞留严重预警→stale 行）；
  毕业缺口（受阻学生总览、课程级明确未通过/待核验、结构性集中待核验、分散待核验，context 补学生行）。
- [x] 四位专家全覆盖（2026-07-22 补齐）：课程质量（总览持续偏高/显著恶化/高影响面→课程清单按状态过滤，
  课程级未通过人数→本学期挂科学生清单，改善课程→改善清单）；
  师资结构（总览大规模/中等规模单人课程→课程清单；课程级聚合值与快照学期不下钻）。
- [x] 下钻标记随简报卡片与问策证据包下发（drillable_facts），旧快照自动失效重建（BRIEFING_SCHEMA_VERSION）。
- [x] 前端：`/admin/verify/signal/:id/detail` 独立只读明细页（新开浏览器标签页）；
  问策右栏数据要素按 drillable 分流——可下钻数字直达明细，其余仍开查证页；查证页关键数字加「明细 ›」入口。
- [x] 回归：后端 174 测试 OK（新增 test_decision_detail 16 项）；生产构建通过；
  浏览器实测：问策→点「队列人数 10人」→新标签页 10 名学生清单（学号/姓名/学院/等级/评分/事由）；
  学院长身份只见本院 14 人明细，范围隔离通过。

---

## P4-09 问策与查证页体验收口（2026-07-22 完成）

用户决策：一种视觉=一种行为；查证页对受众说话；查证解读由 LLM 主笔但按标准骨架与范例约束表达。

- [x] 右栏数据要素分流：可下钻且非零的数字才可点（高亮+›），聚合/判定值与零值渲染为
  静态灰文本（无手型、无边框），查证入口收口到「本轮依据」卡。
- [x] 查证页受众分层：「这个结论是怎么得出的」全部人话化（统计对象只显名称、数据来源
  业务名映射、结论可靠性三档人话、统计口径"标准口径/学校定制口径"）；
  「技术口径（数据管理员）」区（表名/对象ID/配置版本/生成方式）仅 activeRole=admin 可见。
- [x] 查证回答 LLM 主笔：`_VERIFY_TASK` 骨架（结论句→建议→不处理代价）+ 满分范例
  （范例数字不在允许集，照抄必挂数字字面量校验）；未启用/校验失败回退同骨架模板；
  查证指引代码拼接，只引导点击"非零"可下钻数字，不再出现路由路径与 facts 罗列。
- [x] 回归：后端 189 测试 OK（test_decision_llm 新增 LLM 成功/数字校验失败回退用例）；
  生产构建通过；浏览器实测：右栏静态/可点分流正确、点击直达明细新标签页、
  dean 身份不见技术口径区、查证回答骨架结构与「明确未通过」指引正确。
- 遗留：种子库中 admin 账号的唯一身份为 dean（legacy_migration），系统当前无
  activeRole=admin 的账号，技术口径区正向渲染待生产账号补齐后复核。

---

## M1 课程通过率三分层 + 公共必修关注（2026-07-22 完成）

计划见 `docs/91-管理分析增强实施计划.md`。用户决策：两套通过率口径统一走 V2
`grade_attempt`（attempt_type 已完备区分 regular/retake/makeup）；课程类别"公共必修"
用培养方案模块 + V1 `dim_course.category` 合并推导。

- [x] 聚合层：新建 V2 `agg_course_pass_stat`（课程×学期，三率+课程类别，分母 0 存 NULL），
  builder `etl/v2_course_pass_builder.py` 幂等全量重建；迁移脚本
  `code/scripts/migrate_course_pass_stat.py`（建表+首次构建+指标登记，连跑两次幂等）。
  构建 6543 行/1902 门课/23 学期；类别分布：公共必修 89/专业必修 590/选修 653/实践 246/其他 324。
- [x] 接口：`/api/v2/topics/course-quality` 改走 agg 表并新增 `course_group` 过滤；
  `fail_rate` 口径变为首次未通过率并标 deprecated（保留一个版本周期）；
  总览 `coursePassRates` 块（首次/补考/重修/公共必修首次，受限身份输出 null）。
- [x] 前端：课程质量页三分层列 + 类别筛选 +「公共必修课重点关注」区（低于均值高亮）；
  总览新增三分层 KPI 行（末次通过率弱化标 deprecated）；学生分析页同步三分层列。
- [x] 指标登记：`sys_kpi_config` 新增 4 条（module='course_quality'）。
- [x] AI 同步：课程质量 Skill 信号 facts 附加三分层率与课程类别（只加不改既有键）；
  管理要情与决策模拟证据口径同步。
- [x] 回归：后端 202 测试 OK（新增 test_course_pass_stat 13 项）；3 门课 SQL 对账一致；
  生产构建通过；接口冒烟三处（专题/总览/学生分析）与浏览器渲染验证通过。
- 遗留：324 门课落"其他"（无方案模块且无 V1 类别，随数据接入重跑 builder 收敛）；
  agg 为全校口径，受限身份学院级三分层放后续迭代；
  新成绩批次入库后需重跑 builder（M4 纳入跑批链路）。

---

## M2 预警多角色参与：导师/班主任接通 + 分派改造（2026-07-22 完成）

用户决策：分派从硬编码辅导员改为按"学生→有效人员关系"匹配。

- [x] 账号接通：迁移脚本 `migrate_staff_accounts.py`（幂等）从 V2 dim_staff + active
  staff_student_scope 生成 176 个演示账号（导师 99、班主任 77；username=工号、Demo@2026），
  同步 sys_user_role 身份与 sys_user_staff 工号映射，source='staff_migration'。
- [x] 分派改造：公共函数 `alert_assignment.assignees_for_student`——辅导员（班级命中）主责、
  班主任/导师协同、回退学院秘书、保底第一个 counselor；settings.py 三处硬编码与
  migrate_alert_workflow.py 全部改用；新事件写主责+协同多行。
- [x] 存量回填：`migrate_alert_assignee_backfill.py`（幂等）545 事件主责重挂学院秘书、
  新增协同 976 行（班主任 895/导师 81）；原硬编码记录降级保留备查，未物理删除；
  每事件恰好 1 个主责校验通过。
- [x] 回归：210 测试 OK（新增 8 项）；API 实测导师 A/B 隔离（2433 仅见 5 名学生 6 条预警，
  dean 对照 5081 条）、越权事件 404。
- 遗留：演示数据仅 1 个辅导员/1 个学院秘书，5032 事件主责仍为保底 counselor（数据问题，
  随真实带班数据接入收敛）。

## M3 三视角学生学业呈现（2026-07-22 完成）

用户决策：班主任与辅导员一期共用班级视图；导师单独"我的学生"视图。

- [x] 接口：`GET /api/admin/students/my-scope` 按身份返回 scopeKind/view/summary/
  classes/students；GPA/挂科/学分/预警口径全部复用现有学生详情与学生分析同口径 SQL。
- [x] 前端：新页 `students/MyScope.vue`（/admin/students/my）——辅导员/班主任班级卡网格
  可展开学生简表，导师学生明细表+汇总卡，行点击下钻现有 /admin/student/:id；
  与学生成长分析页互链；菜单迁移脚本 `migrate_my_scope_menu.py`（幂等）。
- [x] 回归：215 测试 OK（新增 5 项契约）；生产构建通过；API 实测三类身份+dean 引导空态；
  浏览器实测导师 2433 视图渲染（5 学生、KPI 卡齐全）。
- 遗留：fact_major_req 覆盖不足致学分完成率多为"—"（既有数据问题，evidence 已披露）；
  dean 可见该菜单入口显示引导空态（后续迭代可隐藏）。

---

## M4 ETL 采集监控 + 数据分层收口（2026-07-22 完成）

- [x] etl_run 表（V2 库，迁移脚本 migrate_etl_run.py 幂等）：任务/起止/耗时/状态/行数/
  checks_json/triggered_by；唯一部分索引保证同任务至多一条 running（并发硬约束）。
- [x] 跑批落库：公共 helper etl/run_log.py；run_etl.py、v2_course_pass_builder、
  v2_teaching_loader、v2_grade_loader 入口已包 run_logged（成败均落记录）。
- [x] 接口：data_collection.py——overview/batches/runs（只读）+ trigger（白名单 3 任务、
  etl.trigger 动作权限、409 并发保护、120s 超时、sys_security_audit 审计）。
- [x] 前端：系统管理「数据采集监控」页（概览卡/批次台账/运行历史/手动触发/频率配置说明）；
  菜单迁移 migrate_data_collection_menu.py 幂等，仅 dean 授权。
- [x] 分层文档：docs/92-数据架构分层与指标登记规范.md（贴源/事实/聚合/指标四层 +
  指标登记约定，M1 agg_course_pass_stat 为范例）。
- [x] 回归：227 测试 OK（新增 12）；生产构建通过；迁移连跑两次幂等；
  trigger 实测 success（6543 行/2.7s）+ 非法任务 400 + 无权限 403 + 审计落库。
- 遗留：admin 与 dean 账号共用 role_id=dean，dean 持有 etl.trigger（角色拆分属后续）；
  其余 V2 loader 补 run_logged 与 metric_definition 合流列入 docs/92 §4。

---

## M5 预警后续轨迹一期：同类轨迹分布（2026-07-22 完成）

用户决策：一期只做"历史上同类学生后来怎样了"的历史统计分布，个体风险模型放二期。

- [x] 统计口径：以 fact_alert 记录为观察点按 规则×等级 分桶；T=发生学期，
  T 后无成绩学期的观察点计 pending 不进轨迹三项；解除率/GPA变化分布/新增未通过分布/
  风险升级率/毕业结果分布（real/sim 分开标注）；sample<10 标 low_confidence。
- [x] 接口：GET /api/admin/alerts/trajectory（rule_id/level 可缺省），
  服务端按 student_data_scope 过滤（受限角色只统计范围内学生），methodology 随响应返回。
- [x] 前端：预警详情抽屉内嵌「同类预警后续轨迹」卡（TrajectoryCard.vue），
  口径 popover + 占比条 + 固定披露"历史统计分布，不构成个体预测"。
- [x] AI 同步：alert_priority 信号附加 trajectory 摘要键（只加不改）；
  chat.py/narrative.py 禁区修订为"不编造预测，仅可解释后端返回的统计分布或模型输出"。
- [x] 回归：236 测试 OK（新增 9 项含 SQL 双向对账）；生产构建通过；
  实测 dean R2×严重 sample=708 与 SQL 一致、college_dean=86（范围隔离）、401/权限正确。
- 遗留：演示库全部预警发生在当前学期，轨迹三项真实库恒为 pending（数据现实，
  新学期成绩入库后自动产生分布）；毕业去向分布目前全部为合成数据并标注。

---

## M6 DataTable 标准控件（2026-07-22 完成）

- [x] 组件 components/DataTable.vue：列显隐/列顺序/行密度/每页行数，插槽透传自定义渲染，
  偏好按"用户×页面"存 localStorage（bi_table_pref 键，防御回退）。
- [x] 首批迁移 12 页主要表格（总览/学院详情/课程质量/学生分析/学生列表/我的学生/
  预警列表/课程目标/毕业要求/师资概览/开课保障/调课分析），行为不变；
  后续 23 页迁移清单已列入 docs/91 M6 节。
- [x] 回归：生产构建通过；浏览器实测 3 页（列隐藏刷新保持/恢复默认/顺序调整/
  服务端分页联动）全部生效；最终全量回归 236 后端测试 + 188 项 e2e_check 全过。
- 遗留：外部分页恢复时多一次冗余请求；偏好按本机存储不跨设备。

---

# P5 系统管理收口

## P5-01 账号管理

- [x] 统一身份认证账号映射。
- [x] 账号与教职工工号关联。
- [x] 账号状态和多角色关系。
- [x] 系统管理员默认全校业务数据权限。
- [x] 账号变更进入审计。
- [x] 五项账号治理概览可下钻到对应名单，查询条件与全量概览语义分离。
- [x] 账号列表使用服务端分页、组合筛选和统一表格列配置，多认证来源不重复账号行。
- [x] 账号详情在当前页面核查认证映射、工作身份、人员关联、最近登录和账号治理记录。
- [x] 认证接入检查提供学校字段、生命周期、密钥边界和异常映射实施清单。
- [x] 停用和归档保留身份、范围与审计证据；学校同步账号不把本地密码维护作为常规入口。
- [x] 数据权限定向进入并支持带原账号查询条件返回。
- [x] 分页性能按逻辑、索引和适度数据量验证；原型不生成4万条模拟账号。

详细走查、实现和验收依据见
`docs/100-账号管理UE深度走查与优化方案.md`。

## P5-02 角色与功能权限

- [x] 菜单权限。
- [x] 页面动作权限。
- [x] 导出、规则配置、方案发布等关键权限。
- [x] 多角色身份切换。
- [x] 权限预览。

## P5-03 数据权限

- [x] 完成P2管理页面。
- [x] 支持组织、带班、导师关系查询和核验。
- [x] 同步异常可见。

## P5-04 菜单管理

- [x] 两级树形管理。
- [x] 排序、父子关系和角色授权一致。

## P5-05 指标与口径管理

- [x] 指标名称、公式、数据源、粒度、更新时间和版本。
- [x] 页面引用关系。
- [x] 变更影响说明和回滚。
- [x] 客户确认材料207个稳定指标编号进入统一目录，范围背景和历史兼容记录单列。
- [x] 正式定义、页面引用和分析阈值分层治理，不再混用一张展示配置表。
- [x] 当前挂科学生率与历史挂科经历率的登记口径和Dashboard计算一致。
- [x] 服务端分页、业务域/状态筛选、公共DataTable和指标核查抽屉。
- [x] 页面定义版本与实现版本一致性核查、CSV确认清单导出。
- [x] 详细走查、实现和验收见`docs/101-指标与口径管理UE深度走查与优化方案.md`。

## P5-06 分析方案管理

- [x] 四个真实运行的管理分析能力作为产品标准模板，新学校无需初始化方案数据。
- [x] `sys_ai_skill_config`成为唯一运行真值，旧专家方案表退出页面和运行主链。
- [x] 草稿创建、编辑、真实数据检查、影响预览、发布、停用和回滚形成闭环。
- [x] 学校级、角色级覆盖；未覆盖角色回退产品默认，不保存组织数据范围。
- [x] 发布前检查参数、数据、角色、协议和当前学校数据试运行。
- [x] 方案导入、导出不包含业务数据、组织范围或模型密钥。
- [x] 旧“决策配置”并入分析方案管理，旧路由保留兼容跳转。
- [x] 公共DataTable、友好加载、详情/编辑抽屉和未保存确认符合UE契约。
- [x] 详细走查、实现和验收见`docs/103-分析方案管理UE深度走查与优化方案.md`。

## P5-07 审计日志

- [x] 登录、身份切换。
- [x] 学生详情、名单、导出。
- [x] 权限和菜单变更。
- [x] 指标、规则和分析方案变更。
- [x] AI研判、模拟和正式方案发布。

## P5-08 系统参数

- [x] 学校基础信息和学期参数。
- [x] 模型接入、启停和允许范围。
- [x] 缓存、更新时间和展示参数。
- [x] 不包含预警业务规则。

## P5-09 数据采集监控

- [x] 18类应接入数据源形成版本化统一目录，实际教室占用进入同一接入视图。
- [x] 接入状态与质量校验状态分离，`accepted`不再误报为质检失败。
- [x] 待核验事项只使用采集证据，不再混入教学运行域32项质量问题。
- [x] 新增运行—批次多对多证据，现有2次运行回填6条输入批次关联。
- [x] 首屏按接入、时效、运行和待核验四类管理问题组织，并形成优先事项。
- [x] 数据源、运行、交付清单三个工作区及数据源/运行详情抽屉完成。
- [x] 正式表格接入公共DataTable，具备服务端分页、列设置和显示上限。
- [x] 手动重跑收入口径明确的运维抽屉，350毫秒异步受理并轮询运行结果。
- [x] CSV接入核验清单、服务器路径隐藏、动作权限、白名单和审计边界完成。
- [x] 详细走查、实现和验收见`docs/102-数据采集监控UE深度走查与优化方案.md`。

P5阶段门禁：

- [x] 九个系统管理二级菜单均可进入。
- [x] 系统管理员权限符合确认结论。
- [x] 非管理员不能通过URL或API访问系统管理接口。
- [x] 所有修改有审计记录。

P5数据结构、接口、权限、安全和浏览器验收证据见
`docs/89-P5系统管理收口验收记录.md`。

---

# P6 全量验收、文档与Git收口

## P6-01 自动化检查

- [x] 前端 `npm.cmd run build`。
- [x] 后端完整测试。
- [x] `code/scripts/e2e_check.py`。
- [x] 菜单迁移重复执行测试。
- [x] 权限正向和反向测试。
- [x] AI契约、范围隔离和缓存测试。
- [x] `git diff --check`。

## P6-02 浏览器角色回归

- [x] 校领导。
- [x] 教务处。
- [x] 系统管理员。
- [x] 学院A与学院B。
- [x] 辅导员/班主任A与B。
- [x] 导师A与B。
- [x] 一人多角色用户。
- [x] 无权限测试用户。

每类角色检查：

- [x] 默认首页。
- [x] 两级菜单。
- [x] 筛选和下钻。
- [x] 返回路径。
- [x] 详情和名单。
- [x] 跨学院聚合。
- [x] 导出。
- [x] AI管理要情。
- [x] 决策研判。
- [x] 系统管理准入。

## P6-03 文档

- [x] 更新需求说明。
- [x] 更新技术方案和菜单架构。
- [x] 更新数据权限模型和矩阵。
- [x] 更新数据库表结构。
- [x] 更新API契约。
- [x] 更新使用手册。
- [x] 更新二次开发指南。
- [x] 更新部署和测试说明。
- [x] 记录迁移、兼容和已知边界。

## P6-04 Git收口

- [x] 只纳入本次范围文件。
- [x] 检查未跟踪用户文件未被误提交。
- [x] 按完整模块形成提交。
- [x] 推送 `origin/产品终稿`。
- [x] 记录提交号和最终验收结论。

P6自动化、全角色浏览器、文档和最终交付证据见
`docs/90-P6产品终稿全量验收与交接记录.md`。

---

## 4. 全角色权限验收矩阵

| 测试对象 | 全校详细 | 本院详细 | 他院聚合 | 班级学生 | 导师学生 | 系统管理 |
|---|---:|---:|---:|---:|---:|---:|
| 校领导 | 是 | 是 | 是 | 是 | 是 | 否/按授权 |
| 教务处 | 是 | 是 | 是 | 是 | 是 | 否/按授权 |
| 系统管理员 | 是 | 是 | 是 | 是 | 是 | 是 |
| 二级学院 | 否 | 是 | 是 | 本院 | 本院 | 否 |
| 辅导员 | 否 | 否 | 否 | 所带班级 | 否 | 否 |
| 班主任 | 否 | 否 | 否 | 所带班级 | 否 | 否 |
| 导师 | 否 | 否 | 否 | 否 | 所带学生 | 否 |

“他院聚合”不包含其他学院学生名单、学号、姓名、档案和可反向识别的明细。

## 5. 当前已知风险与处理

| 风险 | 当前情况 | 处理要求 |
|---|---|---|
| 两级菜单信息不完整 | 数据表和Layout已有父子结构，登录菜单查询未返回`parent_id` | P1统一修复契约和树形管理 |
| 角色级范围不适合真实用户 | 同角色人员可能共用同一scope | P2改为用户/人员与业务关系授权 |
| 旧库、V2、AI范围逻辑重复 | 多处存在独立角色白名单和映射 | 建立统一权限上下文后逐步迁移 |
| 辅导员关系数据待确认 | 用户说明会提供辅导员、班主任带班表 | 先确定生产契约，数据到位后ETL接入 |
| 班主任名称匹配存在占位ID | 原型部分数据按姓名映射 | 生产使用稳定人员工号 |
| 事实专题与AI专题重复 | 当前存在多个专题页面和业务页面 | P3归位，P4只保留专家研判 |
| AI对话可能引发口径漂移 | 自然语言可调整分析条件 | 区分临时会话参数与正式发布版本 |
| 系统管理员权限较大 | 已确认默认全校业务数据 | 强化审计、身份提示和高风险操作权限 |

## 6. 执行记录

| 日期 | 阶段/任务 | 结果 | 验收证据 | 遗留 |
|---|---|---|---|---|
| 2026-07-19 | P0-01 | 创建并推送`产品终稿`分支 | `origin/产品终稿` | 无 |
| 2026-07-19 | P0-02 | 创建AGENTS和执行检查表 | UTF-8内容检查、`git diff --check`通过 | 无 |
| 2026-07-19 | P4 | 重构AI管理决策 | 3个首批专家、事件式管理要情、对话式决策研判、权限与追溯验收通过，见`docs/88-P4AI管理决策重构验收记录.md` | 低年级受挫、排课资源优化专家按后续扩展实施 |
| 2026-07-19 | P1 | 完成两级菜单与信息架构 | 4项菜单专项测试、34项后端测试、188项端到端检查、生产构建和多角色浏览器回归通过 | 多身份与细粒度数据权限进入P2 |
| 2026-07-19 | P2-01 | 完成权限现状审计和统一模型设计 | 旧库、V2、AI、班主任/导师关系和辅导员目标契约完成核对 | 辅导员真实带班表和大部分班主任工号待生产数据补齐 |
| 2026-07-19 | P2-02/P2-03 | 建立统一权限上下文、用户身份和人员关系底座 | 两项迁移均连续执行两次；权限专项测试、接口冒烟和前端构建通过 | 业务对象接口全量迁移与权限管理页面继续P2-04/P2-05 |
| 2026-07-19 | P2 | 完成统一数据权限底座、跨院聚合、工作身份和数据权限管理页 | 44项后端测试、188项端到端核对、生产构建和多角色反向接口验证通过 | 辅导员真实带班表和77个临时班主任工号待学校补充 |
| 2026-07-19 | P5 | 完成八个系统管理模块收口 | 63项后端测试、188项端到端核对、生产构建、正式库幂等迁移和逐页浏览器验收通过，见`docs/89-P5系统管理收口验收记录.md` | 统一身份认证协议参数和真实认证回调在生产部署时对接 |
| 2026-07-19 | P6 | 完成产品终稿全量验收与交接收口 | 65项后端测试、188项端到端检查、生产构建、全角色浏览器矩阵和核心交接文档同步通过，见`docs/90-P6产品终稿全量验收与交接记录.md` | 学校统一身份、权威人员关系和生产数据库在实施阶段接入 |
| 2026-07-22 | P4-07 | AI管理决策体验重构（R1–R6） | 去闭环（追踪410退役）、查证窗口、简报Top3首屏、专家问策（SSE+会话存储+越界指路）、菜单收口；158项后端测试、188项端到端、生产构建全通过，见`docs/83-AI管理决策体验重构修改计划.md` | 浏览器实机回归矩阵建议在部署前补一轮；POST `/chat` 一个版本周期后移除 |
| 2026-07-22 | P4-08 | 数据要素明细下钻 | 人数类数字新标签页直达业务明细清单（明细与查证分工）；Skill detail_specs 契约 + detail 接口（服务端鉴权、200行上限）；预警优先级/毕业缺口落地；174项后端测试、生产构建、浏览器动线与学院范围隔离实测通过 | 四位专家明细规格已全部补齐（同批第二次提交），暂无遗留 |
| 2026-07-22 | P4-09 | 问策与查证页体验收口 | 右栏一种视觉=一种行为（非零下钻数字才可点，其余静态灰文本）；查证页口径区人话化+技术口径仅管理员可见；查证回答LLM主笔（骨架+范例+数字校验，回退同骨架模板）；189项后端测试、生产构建、浏览器实测通过 | admin种子账号身份为dean（legacy_migration），技术口径区正向渲染待生产账号补齐后复核 |
| 2026-07-22 | M1 | 课程通过率三分层+公共必修关注 | agg_course_pass_stat（V2统一口径）+迁移脚本；课程质量/总览/学生分析/AI信号四处同步；4条指标登记；202项后端测试、生产构建、接口冒烟与浏览器渲染验证通过 | 324门课落"其他"待数据接入收敛；学院级三分层后续迭代 |
| 2026-07-22 | M2 | 预警多角色参与：导师/班主任接通+分派改造 | 176个演示账号迁移脚本+sys_user_staff映射；按学生关系分派（辅导员主责/班主任导师协同/学院秘书回退）；存量5577条幂等回填；210项测试与导师A/B隔离实测通过 | 演示数据仅1个辅导员，5032事件主责仍为保底counselor |
| 2026-07-22 | M3 | 三视角学生学业呈现 | my-scope接口（辅导员/班主任班级视图+导师学生组视图）；MyScope.vue新页+菜单迁移；215项测试、生产构建、API与浏览器实测通过 | fact_major_req覆盖不足致学分完成率多为—（既有数据问题） |
| 2026-07-22 | M4 | ETL采集监控+数据分层 | etl_run表+跑批落库helper；data_collection三读一触发接口（白名单+审计+并发保护）；数据采集监控页+菜单迁移；docs/92四层架构与指标登记规范；227项测试、构建、trigger实测通过 | admin与dean共用role_id，dean持有etl.trigger；其余loader补run_logged后续 |
| 2026-07-22 | M5 | 预警后续轨迹一期：同类轨迹分布 | trajectory接口（规则x等级分桶、pending口径、范围过滤）+预警详情同类轨迹卡+AI禁区修订；236项测试、构建、dean/college_dean范围隔离实测通过 | 演示库预警均在当前学期，轨迹三项恒为pending；毕业分布为合成数据并标注 |
| 2026-07-22 | M6 | DataTable标准控件 | 列显隐/顺序/密度/行数+偏好持久化；首批12页迁移行为不变；构建+浏览器实测3页通过；最终回归236测试+188项e2e全过 | 剩余23页后续迁移；偏好不跨设备 |
| 2026-07-24 | P3-05 | 完成培养质量分析口径与UE收口 | 四工作区、统一模块规则与预计算摘要、方案覆盖边界、证据抽屉和旧路由兼容完成；275项后端测试、190项端到端检查、生产构建及校级/学院级浏览器回归通过，见`docs/97-培养质量分析UE深度走查与优化方案.md` | 生产系统需接入结构化版本化方案、正式学生方案绑定、选课/缓免认定及下一周期教学计划 |
| 2026-07-24 | P3-06 | 完成师资保障分析口径与UE收口 | 单页课程保障工作台、五类管理指标、异常数据闸门、课程责任组织、最近3次实际开课、课程/教师证据抽屉和旧路由兼容完成；280项后端测试、190项端到端检查、生产构建及校级/学院级浏览器回归通过，见`docs/98-师资保障分析UE深度走查与优化方案.md` | 生产系统需接入正式课程团队及负责人、教师资格与岗位状态、合规年龄段、退休政策和未来开课计划 |
| 2026-07-25 | P3-07 | 完成学生成长与学业分析口径与UE收口 | 单一角色自适应工作台、学分加权GPA、五项管理指标、四类可解释关注组、学生学期与课程结果汇总、公共证据抽屉、URL返回恢复及班级分页表完成；297项后端测试、210项端到端检查、生产构建、五类身份浏览器回归和V0.7外部口径文档同步通过，见`docs/99-学生成长与学业分析UE深度走查与优化方案.md` | 生产系统需确认GPA换算、显著变化阈值、低年级范围，并持续接入完整历史成绩、正式培养方案绑定与预警状态 |
| 2026-07-25 | P5-01 | 完成账号管理治理能力与UE收口 | 五项治理概览、服务端分页筛选、认证接入准备度、账号核查抽屉、安全生命周期、审计降噪和数据权限带条件返回完成；不生成4万模拟账号，按分页逻辑、索引、适度数据量和真实浏览器路径验收，见`docs/100-账号管理UE深度走查与优化方案.md` | 生产部署需对接学校真实认证协议、稳定主体字段和人员生命周期同步 |
| 2026-07-25 | P5-05 | 完成指标与口径管理统一目录与UE收口 | 207个客户指标编号、3项背景/兼容记录统一登记；定义/页面/阈值分层；挂科口径纠偏；页面一致性、分页筛选、证据抽屉和确认清单导出完成；307项后端测试、生产构建、浏览器路径和幂等迁移通过，见`docs/101-指标与口径管理UE深度走查与优化方案.md` | 学校反馈后确认候选口径；生产指标平台接口按学校数据架构接入 |
| 2026-07-25 | P5-09 | 完成数据采集监控可信度与UE收口 | 18项数据源目录、实际教室占用统一接入、状态纠偏、402条真实待核验记录、运行—批次关联、服务端分页、证据抽屉、交付清单和异步运维完成；310项后端测试、生产构建和接口性能门禁通过，见`docs/102-数据采集监控UE深度走查与优化方案.md` | 生产系统由学校数据交换平台提供正式调度和作业编号；源数据问题仍在业务系统修订 |
| 2026-07-25 | P5-06 | 完成分析方案管理运行闭环与UE收口 | 四个产品模板、学校角色覆盖、真实数据发布前检查、影响预览、版本回滚、导入导出和运行时生效完成；315项后端测试、214项端到端、生产构建和真实数据副本闭环通过，见`docs/103-分析方案管理UE深度走查与优化方案.md` | 生产多实例发布需使用学校配置中心事务或分布式锁；模型密钥仍由部署环境管理 |
| 2026-07-25 | P6再认证 | 完成产品终稿最新代码、文档和真实页面再认证 | 315项后端测试、214项端到端检查、2545模块生产构建通过；分析方案管理四类模板、详情、编辑、草稿、版本和未保存提醒经浏览器复核，取消回调错误已修复 | 学校生产接口、统一认证和多实例配置发布继续在实施阶段接入 |
| 2026-07-25 | 学校沟通准备 | 完成电子反馈导航与重点确认清单 | 12项跨模块基础口径、六个业务模块、生产数据接口、权限和四类分析方案形成可填写清单；完整确认书升级V0.13并同步外部技术方案目录 | 等待学校按A/B/C/D/E反馈，收到意见后形成学校版口径与接口清单 |
| 2026-07-25 | PRD优先级校正 | 按已确认优先级升级正式PRD V1.1 | P0为成绩挂科口径、学生成长、培养执行和生产交接底座；P1为风险识别及核心管理分析；P2为AI管理决策；预警干预闭环退出当前高优先级验收 | 等待学校确认正式口径和生产接口后拆分开发迭代 |
| 2026-07-27 | P3-02 07.26修改 | 完成教学数据总览学校—学院—专业—课程—学生证据链改造 | 通用历年指标下钻、专业预警与年级课程、课程最终有效结果、课程学生画像和总GPA完成；324项后端测试、214项端到端、2556模块生产构建及全角色浏览器回归通过 | 历史预警无真实快照的学期继续显示不可用；生产规则仍待学校确认 |
| 2026-07-31 | BR-01 自动化门禁 | 完成“基础报表”第四一级模块、九张只读报表、Excel、权限、菜单/指标迁移和口径登记 | 仓库外可恢复备份；迁移前后42张业务表哈希一致；332项后端测试、214项端到端检查、2562模块生产构建及九路由浏览器回归通过 | 等待用户人工验收；未执行暂存、提交、推送或合并；RPT-04B排名待确认，RPT-06仅累计快照，RPT-08正式名单源未接入 |
| 2026-07-31 | BR-01 原表结构校正 | 九张报表查询条件与结果字段分离；初始空表头；RPT-04A/B四项必选；取消分页；页面/Excel同步原表列、分组表头和合并单元格 | 336项后端测试、214项端到端检查和2562模块生产构建通过；九路由首屏表头、无分页、RPT-04A缺参422、RPT-05全量185行/Excel导出及140组合并浏览器实测通过 | 等待人工验收，仍未暂存、提交、推送或合并 |
| 2026-08-02 | BR-01 RPT-08 同需求改造 | 按用户确认将 RPT-08 与 RPT-07 统一为同一 R2/R2W 名单需求；除报表编号、名称、路由和标题末尾外，共用查询、规则提示、明细、权限状态、快照及 Excel 结构 | 355 项后端测试、2562 模块无压缩生产构建通过；真实 API 同条件业务响应完全一致，RPT-08 Excel 三工作表、标题、规则与版本实测通过 | 等待用户人工验收；未暂存、提交、推送或合并 |
| 2026-08-02 | BR-01 RPT-07/RPT-08 年级可选 | 两张同需求名单报表仅保留学年学期必填；年级留空时查询全部授权年级，选择时按 `entryGrade` 精确过滤；标题、快照和 Excel 同步支持全部年级 | 360 项后端完整测试、2562 模块无压缩生产构建通过；真实 API 已验证无年级查询、指定年级精确筛选及张硕 2 门未解决课程明细；Excel 已验证首行同源标题、`A1:H1` 合并及“全部授权年级”说明 | 等待用户人工验收；未暂存、提交、推送或合并 |
| 2026-08-02 | BR-01 RPT-07/RPT-08 空结果提示调整 | 两张同需求名单报表在 `source_unavailable` 空结果状态下不再显示顶部数据源提示框；后端状态、空结果表和导出禁用规则保持不变，其他基础报表仍保留原提示 | 362 项后端完整测试及 2562 模块无压缩生产构建通过；前后端服务启动后健康检查、登录接口均实测通过 | 等待用户人工验收；未暂存、提交、推送或合并 |
| 2026-08-02 | BR-01 RPT-07 需求文档同步 | 按当前实现逐项校正 RPT-07 的查询条件、精简页面、动态标题与 Tooltip、R2/R2W 明细口径、权限状态、空结果表现、Excel 三工作表、查询快照和回归样例；同步明确 RPT-08 仅保留名称差异 | 两份需求文档交叉核对、关键实现字段检索及 `git diff --check` 通过 | 等待用户检查确认；仓库外确认书在仓库版本认可后再同步覆盖 |
| 2026-08-02 | BR-01 RPT-08 需求文档同步 | 在与 RPT-07 同需求约束基础上，独立补齐 RPT-08 的查询条件、精简页面、8 列明细、R2/R2W 来源、权限与空结果、Excel 三工作表、独立快照和同结果验收标准 | 真实 API 验证同条件两张报表均为 254 人且学生集合一致；张硕、曹雅竹明细复核通过；文档关键契约及 `git diff --check` 通过 | 等待用户检查确认；254 仅为当前演示快照基线，生产人数不固定；仓库外确认书待认可后同步 |
| 2026-08-02 | BR-01 RPT-04B 展示与导出调整 | 页面改用精简布局并将导出移动到结果标题右侧；筛选标签改为“年级”；结果标题按学期、年级、学院、专业及可选班级生成；Excel 首行复用同一标题；空结果仍按授权维表解析学院、专业名称 | 34 项基础报表契约测试、362 项后端完整测试、2562 模块无压缩生产构建及 `git diff --check` 通过 | 等待用户人工验收；未暂存、提交、推送或合并 |
| 2026-08-02 | BR-01 RPT-04B 需求实现同步 | 按当前代码和页面效果重写 RPT-04B 排序范围、学分加权平均分、同分稳定顺序、序号分桶、6 列结果、精简页面、动态标题、权限和三工作表 Excel 需求；同步 V0.14 指标口径确认书 | RPT-04B 文档关键契约检查和 `git diff --check` 通过；确认旧“同分同名次”“导出未纳入排名人数”等表述已移除 | 提交用户检查；未暂存、提交、推送或合并，正式排名与班级筛选语义仍待学校确认 |
| 2026-08-09 | BR-01 当前阶段 GitHub 交付 | 提交基础报表后端、前端、菜单权限、Excel、迁移与回滚脚本、测试、原始报表附件、0728 需求文档及同步口径文档 | 51 项基础报表与菜单测试通过，前端 2562 模块生产构建通过，敏感信息扫描和 `git diff --check` 通过；后端完整 372 项中的 2 项 AI 黄金样例失败已在未修改基线复现，确认与本次基础报表变更无关 | 已创建文件快照和 Git bundle；用户明确授权提交、推送并合并；其他原型、AI 文档和 `work/` 临时文件不纳入本次交付 |
| 2026-08-09 | BR-01 GitHub 脱敏演示初始化 | GitHub 源码检出无教务源库时，自动生成轻量 V1/V2 演示库和 9 学期四级源库；初始化同步基础报表菜单、授权、参数与指标绑定，已有分析库默认拒绝覆盖 | 52 项基础报表、演示初始化和菜单测试通过；隔离目录完成登录、9 报表非空查询及 9 份 Excel 导出验证，行数为 6/3/4/7/2/12/7/24/24；演示姓名、学号、组织和成绩均为构造数据并标记 `demo` | 正式部署仍须接入学校教务源数据；大体量数据库及学校业务数据不进入 Git |
| 2026-08-17 | P3-03 预警规则审核与候选明细调整 | 核对两条待审核变更的账号、权限和操作入口；候选学生弹窗移除新增/保留/退出分类切换及变化列，统一展示全部候选学生；同步详细需求V1.7和确认书V0.22 | 实库只读核查确认变更单#1/#2均为待审核、`quality_office`具有`review`；规则治理可见性3项测试通过；前端2562模块生产构建通过 | 未代用户执行审核；审核后仍需教务处发布、校领导激活 |
| 2026-08-17 | P3-03 规则变更影响交叉分布 | 将学院、专业、年级三个页签合并为学院—专业—年级交叉表，提供动态年级列、专业行、学院小计和全校合计；统计新增、保留、退出全部候选影响；同步详细需求V1.8和确认书V0.23 | 新增交叉聚合2项测试通过，规则治理可见性3项测试通过，前端2562模块生产构建通过；实库变更单#1为1590/1590、#2为165/165，矩阵总计与候选总数一致 | 无；年级或组织缺失记录分别归入未知年级、未知学院/专业 |
| 2026-08-17 | P3-03 影响分析文案精简 | 移除“学院专业年级分布”标题下方重复统计范围说明；统计公式、数据范围和交叉表结构不变；同步详细需求V1.9和确认书V0.24 | 前端2562模块生产构建及本次文件差异检查通过 | 无 |
| 2026-08-17 | P3-03 学业预警监控独立完整需求基线 | 从空白结构新建108详细需求，按预警监控首页、全部下钻、规则治理、低年级观察、公共指标与验收规则完整登记；107保留为历史文档；同步确认书V0.25 | 依据当前前端、后端接口、规则引擎、权限模型逐项核对；文档结构、链接、关键口径和差异检查通过 | 正式GPA、学分进度、规则阈值、在籍口径和生产数据接口仍需学校确认 |

# 培养方案例外认定规则基础能力

## 本批完成

- 新增课程替代关系表 `fact_course_equivalence`。
- 新增学生免修/学分认定表 `fact_student_credit_recognition`。
- 新增培养方案课程组表 `fact_plan_course_group`。
- 合规引擎支持以已通过的替代课程满足目标必修课程。
- 合规引擎仅消费状态为 `approved` 的学生认定记录。
- 已批准的课程认定及模块学分认定可计入方案学分和选修要求。
- 例外应用结果、认定学分和审批依据以结构化字段返回。
- 学业进度详情增加认定学分与已应用例外数量展示。

## 可信边界

当前源数据没有正式的课程替代、免修、转学分或跨专业认定审批数据，因此三张新表保持空表，不生成演示认定。系统已经具备接收和计算这些规则的能力，但只有导入正式审批记录后才会改变学生合规结论。

## 验证

- 数据库迁移完成，三类规则当前均为 0 条。
- Python 编译检查通过。
- 前端生产构建通过。
- 全量自动化回归 78 项通过、0 项失败。
- 新增回归断言覆盖“仅批准记录生效”和学生例外结果字段。

## 下一步

实现课程组最低门数/最低学分计算、学生级合规详情筛选与导出，并补充有审批依据的测试夹具以验证替代和认定对缺口的实际抵扣效果。

# 规则治理自动化与浏览器回归报告

## 自动化覆盖

规则治理专项脚本覆盖：

- 通用候选试算与事务化激活；
- 保留、新增、退出数量对账；
- 跟进记录完整性；
- 风险周期和前序事件关联；
- 数据指纹过期门禁；
- 编辑、审核、发布、激活、审计权限矩阵；
- 参数差异和组织分布聚合；
- 候选 CSV 导出；
- 旧直接修改接口关闭；
- 全站基础接口和数据口径回归。

最终端到端自动化：69 项通过，0 项失败。

## 浏览器回归

### 教务处

- 可以看到系统设置；
- 可编辑规则阈值；
- 编辑弹窗包含数值阈值、变更原因和“创建变更单”；
- 未执行提交操作，生产规则未改变；
- 页面没有浏览器控制台错误。

### 质量办

首次回归发现质量办拥有后端审核权限，但菜单未开放系统设置，导致无法进入审批页。已修复：

- `seed.py` 将系统设置菜单授权给 `quality_office`、`school_leader`；
- `migrate_menu.py` 增加幂等菜单迁移；
- 生产演示库已执行迁移。

修复后验证：

- 质量办可看到系统设置菜单；
- 6 个规则编辑按钮全部禁用；
- 6 个启停开关全部禁用；
- 保留审核与审计职责；
- 页面控制台错误为 0。

### 校领导

系统设置菜单已同步授权，后端仅授予激活和审计权限；激活按钮只会在存在已发布变更单时显示。

## 最终验证

- 后端与测试脚本编译通过；
- 前端生产构建通过；
- `dist` 产物完整；
- 后端服务正常运行；
- 自动化回归 69/69；
- 浏览器未创建变更单、未发布配置、未激活生产预警。

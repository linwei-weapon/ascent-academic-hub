# 通用前端环境变量模板
# 复制为 .env 后按本地环境调整；不要在模板中填写真实密钥。

VITE_VERSION = 3.0.2
VITE_PORT = 3006
VITE_BASE_URL = /
VITE_ACCESS_MODE = frontend
VITE_WITH_CREDENTIALS = false
VITE_OPEN_ROUTE_INFO = false

# 仅填写本地值。VITE_ 前缀变量会打包到浏览器端，不能存放真正的服务端秘密。
VITE_LOCK_ENCRYPT_KEY = replace-with-local-value

# 产品终稿分支执行上下文

## 1. 项目定位

本仓库是“本科教学分析与学业决策支持平台”的可运行原型和生产开发交接基线。

- 面向校领导、教务处、二级学院、辅导员、班主任、导师及系统管理员。
- 系统只消费、分析和挖掘教务数据，不维护学校业务主数据，不回写教务源库。
- 真实数据、推导数据和演示数据必须保留来源标识；数据不足时披露边界，不用假数据伪装正式结论。
- 当前开发分支为 `产品终稿`，基线来自 `AI能力分支` 的 `b72c29b`。
- 当前总执行计划和验收清单见 `docs/82-产品终稿改造执行计划与检查表.md`。

## 2. 已确认且不得擅自改变的产品决策

### 2.1 两类业务能力

1. “教学管理分析”采用预设页面、管理视角、指标维度、图表表格和证据下钻，稳定呈现事实分析。
2. “AI管理决策”采用管理专家 Skill、管理事件卡片、自然语言研判和决策模拟，承载学校个性化管理方法。
3. 不把全部业务页面改造成聊天页面，不在普通表格中平铺逐行 AI 评价。

### 2.2 两级菜单

一级菜单固定为：

1. 教学管理分析
2. AI管理决策
3. 系统管理

二级菜单目标结构：

- 教学管理分析：教学数据总览、学业预警监控、教学运行分析、培养质量分析、师资保障分析、学生成长与学业分析。
- AI管理决策：管理要情、决策研判。决策模拟并入决策研判，不单独作为菜单。
- 系统管理：账号管理、角色与功能权限、数据权限、菜单管理、指标与口径管理、分析方案管理、审计日志、系统参数。

预警规则配置和规则自发现继续位于“学业预警监控”工作区，不迁回系统管理。

### 2.3 数据权限

- 校领导、教务处：查看全校数据。
- 二级学院：查看本学院详细数据；指定页面可以查看其他学院聚合对比，不得下钻其他学院学生明细。
- 辅导员、班主任：按有效“人员—行政班”关系查看所带班级学生。
- 导师：按有效“导师—学生”关系查看所带学生。
- 系统管理员：默认拥有全校业务数据权限，同时拥有系统管理权限。
- 一人多角色时使用“当前工作身份”切换；所有后端接口仍按当前身份和实际授权校验。
- 菜单隐藏不是安全边界。详情、导出、AI研判、证据链接和直接 API 请求都必须在服务端重新鉴权。
- AI不得扩大数据权限；自然语言不能绕过角色、组织、班级或导师关系限制。

### 2.4 现有专题的归属

- 学业风险与低年级受挫 → 学业预警监控 / 学生成长与学业分析。
- 毕业准备核查与课程保障 → 培养质量分析。
- 课程质量与教学运行 → 教学运行分析。
- 资源与师资风险 → 师资保障分析。
- 排课策略与资源优化 → 教学运行分析。
- 上述事实分析可被 AI 管理专家复用，但不得维护两套冲突指标。

## 3. 开始任务前只读什么

不要先通读全部仓库。按任务选择以下最小上下文：

| 任务 | 必读文件 |
|---|---|
| 总体进度与验收 | `docs/82-产品终稿改造执行计划与检查表.md` |
| 菜单、路由、侧栏 | `code/frontend/src/views/admin/Layout.vue`、`code/frontend/src/router/index.ts`、`code/backend/api/routers/auth.py`、`code/backend/api/routers/admin_rbac.py` |
| 登录、角色、数据范围 | `code/frontend/src/store/auth.ts`、`code/frontend/src/store/role.ts`、`code/backend/api/deps.py`、`code/backend/api/routers/v2.py` |
| 班主任、导师数据 | `code/backend/etl/v2_teaching_loader.py` 及相关 V2 schema |
| AI管理要情与模拟 | `docs/81-AI管理能力重构实施方案.md`、`code/backend/api/routers/ai.py`、`code/frontend/src/views/admin/reports/ManagementBriefing.vue`、`DecisionSimulation.vue` |
| 业务页面和路由归属 | `code/frontend/src/views/admin/` 下对应模块、`docs/80-原型最终集成验收与生产交接清单.md` |
| 部署与启动 | `docs/03-部署手册.md`、`code/scripts/start.bat` |

若任务涉及指标，先搜索指标的后端计算、前端展示和文档口径，禁止只改其中一处。

## 4. 主要目录

```text
code/backend/api/routers/    FastAPI业务接口
code/backend/api/deps.py     登录用户与通用数据范围
code/backend/etl/            V2数据结构、ETL和真实数据映射
code/backend/tests/          后端契约与权限测试
code/frontend/src/router/    前端路由和准入
code/frontend/src/store/     登录、角色和范围上下文
code/frontend/src/views/     管理端业务页面
code/scripts/                初始化、迁移、启动和端到端检查
docs/                        产品、技术、验收和交接文档
```

## 5. 修改纪律

1. 开始前确认当前阶段、任务编号、验收条件和涉及文件。
2. 只修改任务范围内的文件，保留用户已有和未跟踪内容。
3. 数据库结构变化必须提供可重复执行的迁移脚本，不直接手工修改唯一数据库。
4. 新旧路由迁移应保留兼容重定向，避免既有链接立即失效。
5. 统一权限解析逻辑，禁止在各 router 中继续复制新的角色白名单。
6. 权限范围缺失时默认拒绝或返回空范围，不得回退为全校；系统管理员是明确授权的全校角色，不属于缺失映射回退。
7. 跨学院比较与学院明细必须使用不同权限语义；“能比较”不等于“能下钻”。
8. AI重要结论必须包含分析范围、数据时间、事实来源、规则版本、证据入口和适用边界。
9. 大模型负责意图理解和解释，不负责临时发明正式指标或未经验证的计算结果。
10. 不扩展为学生工作管理系统，不自动执行开班、排课、课程认定、预警处置或教师评价。
11. 服务启动沿用项目后台服务机制；不要打开可见 PowerShell 窗口作为长期服务。
12. 模块全部完成并通过检查后再统一提交 Git。
13. 每个“教学管理分析”模块完成走查、修改和验收后，必须在同一次模块交付中同步更新 `docs/本科教学分析与学业决策支持平台需求调研及指标口径确认书V2.md`，至少同步指标名称、计算逻辑、管理用途、数据来源、适用边界和页面核查路径；未同步确认书的模块不得标记完成。仓库版本确认后，再同步覆盖 `D:\AI教育\AI学业助手\14-技术方案\本科教学分析与学业决策支持平台需求调研及指标口径确认书V2.md`。

## 6. 检查与验证

按风险选择验证，阶段门禁至少包括：

```powershell
# 前端生产构建
Set-Location code/frontend
npm.cmd run build

# 后端单元测试（按任务选择相关测试，最终阶段运行完整集合）
python -X utf8 -m unittest discover -s code/backend/tests

# 后端服务运行后执行端到端数据与权限检查
python -X utf8 code/scripts/e2e_check.py

# 提交前格式检查
git diff --check
```

浏览器回归至少覆盖：

- 校领导/教务处全校视角。
- 二级学院本院详情和跨院聚合对比。
- 两个不同辅导员或班主任的班级隔离。
- 两个不同导师的学生隔离。
- 系统管理员全校业务数据与系统管理权限。
- 直接 URL、接口、导出、AI对话、证据下钻和角色切换。

## 7. 子任务或子 Agent 交接模板

只有在用户明确要求或当前执行规则允许时才启动子 Agent。委派时不要让其“自行研究整个项目”，必须提供：

```text
任务编号：
目标结果：
允许修改的文件/目录：
必须阅读的最小上下文：
已确认产品决策：
不得改变的边界：
验收命令：
期望回报：修改文件、验证结果、遗留风险；不得自行提交或改动任务外文件。
```

根任务负责跨模块决策、计划状态更新、最终回归和 Git 提交。并行任务不得同时修改同一文件、同一数据库迁移或同一公共权限函数。
